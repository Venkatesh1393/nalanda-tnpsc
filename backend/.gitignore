node_modules/
dist/
coverage/

.env
.env.local
.env.*.local

logs/*.log

*.tsbuildinfo

# Sprint 4 Step 73 — Production Cloud Services. Real user data dumps
# (scripts/backupDatabase.ts) — never committed, same reasoning as .env.
backups/
