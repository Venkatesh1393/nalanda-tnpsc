# Nalanda TNPSC — Master Roadmap

| | |
|---|---|
| **Document Owner** | Engineering |
| **Status** | Living document — update alongside every implementation session |
| **Last Updated** | 2026-08-04 (Sprint 4 Step 52 — Admin Portal Foundation, PASS: `admin/` app real for the first time, backend `super_admin` role + centrally-RBAC-gated `/admin/*` routes + `AuditLog` model, real Admin Dashboard stats + User Management; see Phase 10 below and `docs/SPRINT_4_STEP_52_COMPLETION_REPORT.md`. Also 2026-08-04, earlier the same day: real Tamil translations wired into all 8 authenticated app modules [Dashboard/Learn/Practice/Live Exams/Current Affairs/Analytics/Settings/Notifications] — see Phase 14 below. Previously: 2026-08-03 Sprint 3 Step 51 — complete end-to-end audit, PASS: 63/63 real assertions against live Atlas/Cloudinary across the full user journey; see `docs/SPRINT_3_COMPLETION_REPORT.md` and the Phase 13 entry below. Also same day: real Cloudinary file/image storage — Step 50; and real Current Affairs + Leaderboard + Notifications backend: `CurrentAffair` gained tags/image metadata/embedded quiz/related-questions and a full real read module incl. bookmarks; `leaderboard` module computes real Weekly/Monthly/Overall rankings live from `PracticeSession` with a documented scoring formula, Landing Page Top Rankers now reads the same source; `notifications` module real end to end — 6 categories incl. new `achievement`/`system-notice`, real broadcast fan-out, read/unread/mark-all/delete/pagination) |
| **Relationship to `PROJECT_CONTEXT.md`** | `PROJECT_CONTEXT.md` explains *what the product is and why* (architecture, decisions, cross-document consistency). This document tracks *what's actually been built, phase by phase, and what's left* — read it when you need to know "is X done yet" or "what should I build next," not to relearn the product's rationale. |
| **How to keep this current** | After any implementation session, update the phase(s) touched: move items from Remaining → Completed, add newly-discovered remaining work, bump **Last Updated**. Never leave a phase marked complete if a later session's tests/lint/build didn't actually pass. |

---

## Status at a Glance

| Phase | Area | Status |
|---|---|---|
| 0 | Documentation (20 docs in `docs/`) | ✅ Complete |
| 1 | Frontend foundation (tooling, theming, design tokens) | ✅ Complete |
| 2 | Reusable component library | ✅ Complete |
| 3 | Public site chrome (Navbar, Footer) | ✅ Complete |
| 4 | Auth & Onboarding (UI + backend) | 🟡 Real Firebase Auth backend live (2026-08-01: Google + Email/Password + MongoDB user sync + Nalanda JWT sessions); Onboarding wizard UI still mocked; backend Email OTP module still mocked |
| 5 | Backend foundation (Express, MongoDB, models) | 🟡 Foundation scaffold + live MongoDB Atlas connectivity + 24 production Mongoose models/seed system + real domain modules — Auth (2026-08-01), Users/Onboarding/Dashboard (2026-08-02), Learning (2026-08-02), Smart Practice/Bookmarks-extended (2026-08-03, Step 46), Weekly Live Exam (2026-08-03, Step 48), Analytics (2026-08-03, Step 47), Current Affairs/Leaderboard/Notifications (2026-08-03), Cloudinary file/image storage (2026-08-03, Step 50) — all live-verified against Atlas/Cloudinary, re-confirmed by the Step 51 end-to-end audit; only Payments has no repositories/services/controllers/routes now |
| 6 | Public Website pages (Landing, Pricing, Exams, About, Blog) | 🟡 Landing Page complete (Top Rankers now real); Pricing/Exams/About/Blog not started |
| 7 | Student Dashboard core (Learn, Practice, Analytics) | 🟡 Dashboard home screen + full Learn module (real) + Smart Practice engine (Topic Quiz mode real, Step 46; 100 Questions/Sectional/Mock/PYQ still mocked) + Weekly Live Exam (real, Step 48) + full Current Affairs module (real — see below) + Analytics dashboard (10 charts, real — Step 47) complete; Analytics' full 14-view drill-down (Subtopic level, Heatmaps, Consistency calendar, AI Recommendation synthesis) + dashboard-layout not started |
| 8 | Commerce (Payments, Subscriptions, Razorpay) | ⬜ Not started |
| 9 | Engagement (Community, Notifications, Bookmarks, Settings) | 🟡 Notification Center now real (6 categories, real broadcast) + Profile/Settings (mocked); Community, cross-module Bookmarks not started |
| 10 | Admin Panel | 🟡 Foundation (Step 52) + Questions/Bulk Import (Step 53) + full CMS — Exams/Subjects/Topics/Subtopics/Lessons/Study Materials/Current Affairs/Weekly Live Exams (Step 54), all 2026-08-04, all real and RBAC-gated; Subscriptions dashboard, Analytics drill-down, Settings, Institutional/B2B Management, Moderation not started |
| 11 | Mobile App | ⬜ Not started |
| 12 | AI Orchestration Layer | ⬜ Not started |
| 13 | Infra / DevOps / Testing | ⬜ No test runner installed yet; Step 51's 2026-08-03 audit (`docs/SPRINT_3_COMPLETION_REPORT.md`) substituted real throwaway-script E2E verification against live Atlas/Cloudinary — thorough, but still not a repeatable CI suite |
| 14 | i18n (real Tamil/English content) | 🟡 Real Tamil translations wired into all 8 authenticated app modules (Dashboard/Learn/Practice/Live Exams/Current Affairs/Analytics/Settings/Notifications, 2026-08-04) via `frontend/src/i18n/`; Auth/Onboarding/Landing/common were already done; date/time formatting (`toLocaleString('en-IN', ...)`) stays English-locale everywhere, a known follow-up gap |

---

## Phase 0 — Documentation ✅

All 20 product/design/architecture docs in `docs/` (PRD through Smart_Practice.md), plus `PROJECT_CONTEXT.md` as the master summary and this file. Self-consistent; see `PROJECT_CONTEXT.md` §13 for the two known cross-document ordering notes (both already resolved in favor of the newer doc).

---

## Phase 1 — Frontend Foundation ✅

- React 19 + Vite + TypeScript + TailwindCSS v4 + shadcn/ui scaffolded via `npx create-vite` + `shadcn init` (style `radix-nova`, baseColor `neutral`).
- Every design token from `UI_Design_System.md` (§4 type scale, §5-§12 color system incl. dark theme, §22 radius, §19 motion durations) implemented in `src/index.css` and Tailwind's `@theme inline` block.
- `ThemeProvider`/`useTheme` (dark/light/system, persisted).
- React Query, React Router, Axios client (`api/client.ts`), validated env loader (`lib/env.ts`, Zod-backed).
- ESLint (flat config, typescript-eslint + react-hooks incl. React Compiler rules + react-refresh) + Prettier (`prettier-plugin-tailwindcss`), with `components/ui/` excluded from both per the "CLI-owned" convention.

---

## Phase 2 — Reusable Component Library ✅

**26 shadcn/ui primitives** generated via `npx shadcn add` into `components/ui/` (CLI-owned, never hand-edited except where noted):
Button *(edited: added `ai` variant, `premium` variant, `loading` prop)*, Card *(edited: `interactive` prop)*, Input, Label, Badge *(`success`/`warning`/`premium` variants)*, Table, Dialog, Drawer, Sonner *(edited: swapped the CLI's default `next-themes` import for this project's own `useTheme`)*, Checkbox, RadioGroup, Switch, Slider, Progress, Skeleton, Tabs, Accordion, Tooltip, Popover, Avatar, Breadcrumb, Pagination, Select, DropdownMenu, Command, InputOTP *(edited 2026-07-30: forwardRef-wrapped so `components/inputs/otp-input.tsx` can imperatively refocus box 1 after a wrong attempt or a resend, per `docs/OTP_Flow.md` §2/§4/§6)*, Textarea, InputGroup.

**~21 hand-authored composites** in `components/` (business-aware, so deliberately outside `ui/`):

| Category | Components |
|---|---|
| Loading | `Spinner` |
| Cards | `FeatureCard`, `SubjectCard` (covers "Course Card" too — one component, no duplication), `QuestionCard`, `StatCard`, `PremiumCard` |
| States | `EmptyState`, `ErrorState`, `NotFound` |
| Navigation | `BreadcrumbTrail` |
| Inputs (`inputs/`) | `SearchInput`, `PasswordInput`, `OtpInput`, `Autocomplete`, `OptionCard`, `ToggleChip` (the latter two added 2026-07-30 for Onboarding — domain-agnostic "pick a card/chip" primitives, reusable by any future feature) |
| Data | `data-table/DataTable`, `DataTableColumnHeader`, `DataTablePagination` |
| Charts (`charts/`) | `CircularProgress` (generic ring — `ScoreRing` now composes it), `ChartContainer`, plus the pre-existing `ScoreRing`/`ChartTooltip`/`TrendLineChart` |
| Brand | `Logo` (default + `inverted` mode for dark surfaces), `AiOrb` (added 2026-07-30 — extracted out of the Landing Page Hero's inline AI orb so Onboarding's Generate-Plan step could reuse the identical visual, per `docs/Onboarding.md` §6; Hero now imports it too instead of its own copy) |

**New dependencies added:** `vaul` (Drawer), `sonner` (Toast), `input-otp`, `cmdk` (Command/Autocomplete), `@tanstack/react-table` (DataTable). `next-themes` was pulled in transiently by the Sonner template and removed once no longer referenced.

**Verification:** `typecheck`/`lint`/`format:check`/`build` all clean; every component has a live preview section in `App.tsx` (both themes).

---

## Phase 3 — Public Site Chrome ✅

**Navbar** (`components/layout/navbar.tsx`): sticky, scroll-driven transparent→solid transition (`useScrolled` hook), responsive (full row ≥`md`, hamburger + `Drawer` sheet below it), animated link stagger-in + underline hover, Logo, Language toggle, Theme toggle, Login/Register/Pricing(premium-styled) actions. Links to Current Affairs/Learn/Smart Practice/Weekly Live Exam/Analytics/Community as landing-page anchors (`ROUTES.homeSection()`), since those modules don't have pages yet.

**Footer** (`components/layout/footer.tsx`): brand column (inverted `Logo`, real newsletter form with local validation/submit-state/toast feedback, social-link chips), Company/Courses(from the real `EXAM_CATEGORIES` constant)/Resources link columns collapsing into an `Accordion` on mobile, bottom bar with copyright + Privacy/Terms + a low-emphasis repeat "Get Started" link + a Razorpay trust line.

**Supporting additions:** `LanguageProvider`/`useLanguage`/`language-context.ts` (real persisted EN/TA preference, mirrors the Theme pattern — no translated content wired to it yet), `useScrolled` hook, `--footer`/`--footer-foreground`/`--footer-primary` CSS tokens (mirrors the existing `--sidebar` pattern), `ROUTES` additions (`pricing`, `about`, `contact`, `blog`, `privacy`, `terms`, `examLanding()`, `homeSection()`).

Both components are mounted live in `App.tsx`, wrapping the existing component preview — not just demoed in isolation.

**Known-open items from this phase:**
- ~~Every link the Navbar/Footer render points at a path with no page behind it yet~~ — `/`, `/login`, `/register`, `/verify-email`, `/app/dashboard` now resolve to real pages (Phases 4 & 6). `/pricing`, `/about`, `/exams/:slug`, `/blog` still don't.
- Social links are `#` placeholders — no real profile URLs exist yet.
- Newsletter form has no backend endpoint — confirms locally via toast only.
- ~~`layouts/website-layout.tsx` hasn't been built~~ — built in Phase 6, now assembles `<Navbar/><AnimatedOutlet/><Footer/>`.
- The Navbar's desktop link row needed its breakpoint moved from `md` to `xl` during Phase 6 testing — the 6-label row plus language/theme/3 CTAs doesn't fit below ~1280px without wrapping.

---

## Phase 4 — Auth & Onboarding 🟡

Per `Authentication.md`, `Registration_Flow.md`, `OTP_Flow.md`:
- [x] Frontend: Login (Google + Email OTP, Remember Me), Register (3-path "Choose Your Path" selector; Individual/Coaching-Student share a details form, Institution Owner routes to an honest "talk to sales" state instead of a faked B2B flow), shared Verify Email OTP screen (`OtpInput` from Phase 2, wrong/expired/lockout/resend all implemented per `OTP_Flow.md`'s exact rules).
- [x] **2026-07-30 hardening pass on the OTP module** (`components/inputs/otp-input.tsx`, `features/auth/components/otp-verification-form.tsx`) to close gaps against `OTP_Flow.md`'s full spec: auto-focus on mount (opt-in prop, not default-on, so it doesn't fight `App.tsx`'s side-by-side preview instances) + `autoComplete="one-time-code"`/`inputMode="numeric"`/digits-only `pattern` for OS autofill (§2–§3); a real "final 10%" expiry-warning line, with expiry now discovered only at submission time rather than pre-emptively disabling the boxes on a silent countdown (§5, §7); a distinct `network-error` outcome that preserves the entered code and offers inline retry instead of shaking it away (§10), behind an 8s request timeout backstop (§8); resend now correctly bypasses its cooldown immediately after an expired submission (§4) — the initial cooldown window also had a bug (it was reading the full OTP TTL instead of the shorter resend-cooldown constant); a distinct `locked` visual (muted/grayscale + padlock overlay) instead of reusing the wrong-code error state (§6); the success checkmark now draws in and holds ~450ms before advancing instead of navigating away instantly (§9); a slim indeterminate progress bar beneath the boxes specifically for auto-triggered verification, matching the doc's "no button in focus to host a spinner" case (§8); and 44px `pointer-coarse:` touch targets on the boxes (`docs/UI_Design_System.md` §31). `components/ui/input-otp.tsx` (CLI-owned) was forwardRef-wrapped to support the imperative refocus-after-error/-resend this required.
- [x] `layouts/auth-layout.tsx`.
- [x] Session state (`context/auth-context.ts`, `providers/auth-provider.tsx`, `hooks/use-auth.ts` — mirrors the Theme/Language pattern) and route guards (`routes/protected-route.tsx`, `routes/public-only-route.tsx`).
- [x] `pages/dashboard/dashboard-page.tsx` — a deliberately minimal, self-labeled placeholder so `ProtectedRoute` has something real to guard; not the real Dashboard (Phase 7).
- [x] **2026-08-01 Real Firebase Authentication (Google + Email/Password), Nalanda session issuance, and MongoDB user sync** — Sprint 3's Firebase step. Full detail below and in `docs/PROJECT_CONTEXT.md` §14; summary:
  - **Backend** (`backend/src/`): `config/firebase.ts` extended with `verifyFirebaseIdToken` (safe, generic-error-on-failure wrapper around Admin SDK `verifyIdToken`) and hardened malformed-config handling (never logs key material). New `middleware/verifyFirebaseToken.middleware.ts` (verifies a client-supplied `{firebaseIdToken}` from the request body, distinct from `authenticate`'s Nalanda-JWT check). New `models/Session.model.ts` (one doc per refresh token/device — not in `docs/Database.md`'s original catalog, added because `Users.refreshTokenHash` can't represent multi-device sessions or rotation/reuse-detection, both required by `docs/Authentication.md` §5/§8). New repositories (`user`, `profile`, `session`), services (`services/auth/userSync.service.ts` — find-or-create by `firebaseUid`, same-email account-linking fallback; `services/auth/session.service.ts` — issue/rotate/revoke with full reuse-detection; `services/auth.service.ts` — orchestration), validators, controller, and `routes/auth.routes.ts` mounted at `/api/v1/auth`: `POST /google`, `POST /email` (both `{firebaseIdToken, rememberMe}` in, `{accessToken, user, isNewUser}` out, refresh token set as an HttpOnly/SameSite=Strict cookie scoped to `/api/v1/auth`), `POST /refresh`, `POST /logout`, `POST /logout-all`, `GET /me` (new — a minimal protected endpoint, added specifically so there's something real for Phase 8's Test D to exercise). `AUTH_PROVIDERS` renamed `email` → `email_otp` + added `email_password`. `cookie-parser` added.
  - **Frontend** (`frontend/src/`): `lib/firebase.ts` (Firebase Web SDK init, env-var-only config — `VITE_FIREBASE_STORAGE_BUCKET`/`MESSAGING_SENDER_ID` added to `lib/env.ts` and `.env.example`/`.env.local`), `services/firebaseAuthService.ts` (the one file importing `firebase/auth` directly — register/login/Google-popup/sign-out/auth-state-listener/ID-token-fetch, with a Firebase-error-code-to-friendly-message map). `services/authService.ts` is now a deliberate **hybrid**: `loginWithGoogle`/`registerWithEmailPassword`/`loginWithEmailPassword` are real; the Email OTP functions remain mocked (the custom backend OTP module, `docs/Authentication.md` §2, was explicitly out of this step's scope). `api/client.ts` gained the response interceptor its own long-standing TODO comment described — silent-refresh-on-401 with single-flight de-duping. `providers/auth-provider.tsx` was rewritten: **no more client-side storage of the access token** (fixes a previously-flagged localStorage inconsistency against `docs/Authentication.md` §4's in-memory-only rule) and a real silent-refresh-on-mount so a page reload restores a session from the refresh cookie alone.
  - **Email/Password was added *alongside* Google + Email OTP, not in place of either** — this session's build instruction initially described Email/Password in a way that conflicted with `docs/Authentication.md`'s passwordless foundation; the user was asked directly and chose to add it as a genuine third option rather than replace OTP or revert to Email/Password-only. `LoginPage`/`RegisterPage` now have a tab switcher (Email code / Password) on top of the unchanged OTP flow. `AUTH_PROVIDERS`/`docs/Authentication.md` itself were **not** rewritten to un-declare the passwordless design — that foundational claim is now only *mostly* true (see `docs/PROJECT_CONTEXT.md` §13 for the exact caveat), the same kind of scoped exception the doc already carries for institutional staff-invite passwords.
  - **MongoDB user sync** (`services/auth/userSync.service.ts`): Firebase is identity, MongoDB is the system of record — a verified token's `uid` is looked up against `User.firebaseUid`; on no match, a same-email fallback lookup handles the (rare, since Firebase's own default project setting already prevents it) cross-provider duplicate case; otherwise a new `User` + `Profile` are created. No password is ever received by or stored in the backend — Firebase verified it entirely client-side before the ID token was even issued.
  - **Verified**: backend `typecheck`/`lint`/`format:check`/`build` and frontend `typecheck`/`lint`/`format:check`/`build` all clean. Backend booted live against the real Atlas cluster and was smoke-tested: `POST /auth/google` with a bogus token → `401` with a generic message (no Firebase internals leaked); missing `firebaseIdToken` → `400` with a clear Zod validation error; `GET /auth/me`/`POST /auth/refresh` with no credentials → `401`; logs confirmed to contain no key material even when the (placeholder) Firebase config failed to decode. **No automated test runner exists for either app** (`backend/tests/`, `frontend/tests/` are still just READMEs) — "run backend/frontend tests" for this step means the lint/typecheck/build trio above; there is nothing else to run yet. Manual, credential-dependent testing (real Firebase Console/MongoDB Atlas verification, Google popup, full register→login round trip) is the user's to run — see the checklist delivered alongside this update.
  - **Not done, out of this step's explicit scope**: Cloudinary, Razorpay, AI, and the real backend Email OTP module (still mocked, unchanged).
- [x] **2026-07-30 Onboarding wizard** (`pages/onboarding/onboarding-page.tsx`, `features/onboarding/`) — reachable at the new protected route `/app/onboarding`, wired so a **new** registration (both the Email-OTP and Google paths on `RegisterPage`) redirects here instead of straight to the Dashboard; an existing-user Login is unaffected. Eight steps: Language → Exam → Target Month → Study Hours → Weak Subjects → **Learning Style** → Generate AI Study Plan → **Completion** — the first five plus Generate-Plan match `Onboarding.md`'s canonical order; **Learning Style and the separate Completion screen are this session's additions, not in `Onboarding.md`/`Onboarding_Personalization_Flow.md`** (those docs weren't edited to match — see `PROJECT_CONTEXT.md` §14 for this open deviation, and confirm with the user whether to reconcile the docs to 8 steps or the code back to 6 before this goes further). Built on two new reusable, domain-agnostic inputs (`components/inputs/option-card.tsx`, `toggle-chip.tsx`), a localStorage-backed `useOnboardingWizard` hook (resumable mid-wizard, flags completion so a stale link redirects straight to the Dashboard instead of restarting), and a mocked `services/onboardingService.ts` (`POST /study-plans`). The Generate-Plan step reuses a newly-extracted `components/ai-orb.tsx` — pulled out of the Landing Page Hero (which now imports it too) per `Onboarding.md` §6's requirement that the exact same AI visual reappear there. **2026-08-02 update (Step 44, Phase 5 entry below)**: the wizard is now real, not `localStorage`-only — every step's answer is persisted to `Profile.onboarding` via `PATCH /onboarding`, the wizard's initial state now hydrates from `GET /onboarding` on mount, and completion (`POST /onboarding/complete`) is a real, awaited network call gating navigation, not a synchronous local flag flip. `generateStudyPlan`/`POST /study-plans` itself is still mocked (no AI Orchestration Service exists yet, Phase 12).

Was originally scoped down from a conventional email+password module (password reset, mobile verification) to strictly match this doc's passwordless design — confirmed with the user rather than assumed, see `PROJECT_CONTEXT.md` §6. **2026-08-01 update**: a real Firebase Email/Password path was added back in, but deliberately *alongside* Google + Email OTP rather than replacing them, and still with no password-reset flow (Firebase's own "forgot password" email-reset UI wasn't wired up either, since it wasn't requested) — see the Firebase Authentication entry above and `PROJECT_CONTEXT.md` §13/§14 for the full reasoning.

Real backend Auth existing now unblocks most other features that assume a real authenticated session — the next highest-leverage step is almost certainly a real domain module (Learn/Practice/etc. repository+service+controller+routes) built on top of it, or the backend Email OTP module if passwordless parity matters more.

## Phase 5 — Backend Foundation 🟡 (cross-cutting scaffold built; domain work not started)

Per `Architecture.md` §2, `FolderStructure.md` §2, `Database.md`:
- [x] **2026-07-31 Backend foundation scaffold** (`backend/`, previously an empty directory) — Sprint 3 Step 41. A working, buildable Express/TypeScript API skeleton with every cross-cutting concern in place, ready for domain modules to build on top of. Folder layout intentionally flattens `FolderStructure.md` §2's `src/api/{routes,controllers,validators}` nesting to top-level `src/{routes,controllers,validators}` (a direct build-instruction reshaping, same "narrows/reshapes doc scope, doc left as-is" precedent `frontend/`'s Onboarding/Notifications deviations already set) — actual layout: `src/{config,constants,controllers,middleware,models,repositories,routes,services,types,utils,validators}` plus `docs/`, `tests/`, `logs/` at the `backend/` root.
  - **Stack installed and wired**: Express 4, Mongoose 8, `jsonwebtoken`, `firebase-admin`, Zod, Winston, Morgan, Helmet, CORS, Compression, `express-rate-limit`, `dotenv`, Multer (v2, memory storage — v1 was deprecated/vulnerable, upgraded during this build), Cloudinary. TypeScript (`~6.0.2`, matching `frontend/`'s pin), ESLint (flat config, `eslint.config.mjs` — `.mjs` extension deliberately, to avoid Node's `MODULE_TYPELESS_PACKAGE_JSON` warning without adding `"type": "module"` to `package.json`, since the compiled output must stay CommonJS) + Prettier, mirroring `frontend/`'s exact style (`semi: false`, single quotes, `printWidth: 90`). `tsconfig.json` uses `module`/`moduleResolution: "Node16"` — plain `"Node"` is deprecated as of this TypeScript version and errors without an explicit opt-in; `"Node16"` is the supported modern equivalent for a CommonJS-output Node backend.
  - **`config/env.ts`** — Zod-validated environment loader, exact same "parse once at import, throw loudly on boot if invalid" pattern as `frontend/src/lib/env.ts`. Validates `MONGODB_URI`, base64-encoded RS256 JWT key pairs (access + refresh, distinct keys — never shared), `JWT_*_EXPIRES_IN`/`JWT_ISSUER`, Firebase service-account fields, Cloudinary credentials, rate-limit window/max, log level. `.env.example` documents every variable with generation instructions (`openssl genrsa`/`base64` for the JWT keys) in `backend/README.md`.
  - **`config/logger.ts`** — single Winston instance, JSON to `logs/error.log`/`logs/combined.log`, colorized human-readable console in development / JSON console in production, plus a `morganStream` adapter so Morgan's HTTP request logs flow through the same instance rather than a separate `console.log`.
  - **`config/database.ts`** (Mongoose connect/disconnect, connection-event logging) and **`config/firebase.ts`** (lazy-initialized Firebase Admin app, decodes a base64 service-account private key) and **`config/cloudinary.ts`** (SDK config) — one adapter each, per `FolderStructure.md`'s "every third-party SDK gets exactly one adapter" rule; no controller/service should import `mongoose`/`firebase-admin`/`cloudinary` configuration directly.
  - **`middleware/auth.middleware.ts`** (`authenticate`/`optionalAuthenticate` — verifies the Nalanda-issued RS256 access token via `utils/jwt.ts`, never a raw Firebase token, per `Authentication.md` §4) and **`middleware/rbac.middleware.ts`** (`authorizeRoles`/`authorizeTiers`, kept as two separate functions per `Authentication.md` §6's "role and subscription tier are independent claims, independent middleware checks, never collapsed" rule) — both new files, not explicitly named in this session's build instruction but added to satisfy the doc's own SOLID/one-middleware-per-concern mandate once `authenticate` existed.
  - **`middleware/validate.middleware.ts`** — Zod-schema request validation (`{ body, query, params }`), the intended consumer of the not-yet-built `validators/` folder.
  - **`middleware/errorHandler.middleware.ts`** (global handler — recognizes `ApiError`, Mongoose `ValidationError`/`CastError`/duplicate-key `11000`, and falls back to a generic message in production so internals never leak) + **`middleware/notFound.middleware.ts`** (404 for unmatched routes) + **`middleware/rateLimiter.middleware.ts`** (`defaultRateLimiter` app-wide, `createRateLimiter` factory for stricter per-route limits like OTP request/verify) + **`middleware/upload.middleware.ts`** (Multer, memory storage, 5MB/image-mimetype limits — buffers only, no local disk write, since the backend is meant to be stateless/autoscaled per `Architecture.md`).
  - **`utils/ApiError.ts`** (typed error class with static factories — `badRequest`/`unauthorized`/`forbidden`/`notFound`/`conflict`/`unprocessable`/`tooManyRequests`/`internal`) and **`utils/ApiResponse.ts`** (`sendSuccess`/`sendError` — the only place the `{ success, data, error, meta }` envelope from `API.md` gets constructed) and **`utils/asyncHandler.ts`** (wraps async route handlers so a rejected promise reaches `errorHandler` instead of crashing the process) and **`utils/jwt.ts`** (RS256 sign/verify for access + refresh tokens, `jti` via `crypto.randomUUID()`, refresh tokens carry a `familyId` ready for the reuse-detection rotation `Authentication.md` §5 specifies — the rotation/revocation logic itself is a future Auth-module service, not built here).
  - **`constants/roles.ts`** (the 5-role/4-tier enums from `Authentication.md` §6, exported as literal-union types) and **`constants/httpStatus.ts`** (named status codes). **`types/jwt.types.ts`** (access/refresh payload shapes), **`types/apiResponse.types.ts`** (the envelope's TS types), **`types/express.d.ts`** (augments `Express.Request` with an optional `user: AccessTokenPayload`).
  - **One vertical slice proves the wiring end-to-end**: `routes/health.routes.ts` → `controllers/health.controller.ts` → `routes/index.ts` (the single mount point, wired at `/api/{API_VERSION}`) → `app.ts` (pure Express assembly — Helmet/CORS/Compression/JSON parsing/Morgan/rate-limiter/routes/`notFound`/`errorHandler`, no `listen()` call, so the app is importable by a future test suite) → `server.ts` (the actual process entry point — connects Mongo, starts listening, graceful shutdown on `SIGTERM`/`SIGINT`, `uncaughtException`/`unhandledRejection` handlers). `GET /api/v1/health` reports process uptime and live Mongo connection state (`mongoose.ConnectionStates[...]`) — unauthenticated, no other data exposed.
  - **`models/`, `repositories/`, `services/`, `validators/` are intentionally still empty** (README.md placeholder in each, explaining the convention and pointing at `FolderStructure.md`) — no domain (User, Auth, Learn, ...) was in this step's scope; a future Sprint 3 step adds the first real vertical slice (almost certainly Auth, since everything else depends on it) through all four layers plus `models/`. **`backend/tests/`** similarly documents the `unit/integration/contract` convention but has no test runner installed yet — none was named in this session's requested stack, so one wasn't assumed; a future step should pick Vitest or Jest and confirm with the user first, same as the still-open `frontend/` testing gap (Phase 13).
  - **Known, accepted risk**: `npm audit` reports 8 moderate-severity advisories, all transitive through `firebase-admin`'s Google Cloud SDK dependency chain (`gaxios`/`google-gax`/`teeny-request` → an old `uuid` with a buffer-bounds edge case only triggered by a call pattern this codebase never uses — passing a raw buffer into `uuid` v3/v5/v6). Tried upgrading `firebase-admin` to `^14.2.0` to clear it — this made the tree *worse* (12 vulnerabilities, 7 high, via a different transitive `glob`/`brace-expansion` chain), and npm's own suggested "fix" for the original 8 is a *downgrade* to `firebase-admin@10.3.0`. Reverted to `firebase-admin@^12.6.0` (the original 8-moderate baseline) rather than trade up for a worse tree — this is an upstream `firebase-admin` packaging problem, not fixable by a version bump in either direction from this repo. Revisit only if `firebase-admin` itself ships a cleaner dependency tree.
  - **Verified**: `npm install`, `npm run typecheck`, `npm run lint`, `npm run format:check`, `npm run build` all pass clean (zero errors, zero warnings) as of this session. Not run: `npm start`/`npm run dev` against a real `.env` (no real MongoDB Atlas cluster, Firebase project, or Cloudinary account provisioned yet — same Phase-13 gap `frontend/`'s `.env.local` placeholder already has) — build/compile success was verified, not a live-boot.
- [x] **2026-08-01 MongoDB Atlas connectivity verified live** (Sprint 3, immediately following Step 41). A real Atlas cluster + `nalanda_tnpsc` database now exist; `config/database.ts` was extended and confirmed to actually connect, not just compile:
  - **Retry logic hardened**: `connectDatabase()` retries the initial connection up to 5 times with exponential backoff (1s → 2s → 4s → 8s, capped at 15s), logging each attempt via Winston, and rethrows (fatal, `server.ts` exits) only after exhausting all 5 — this path was exercised for real during verification (see the DNS issue below) before the fix landed, confirming the backoff/logging behavior works as designed, not just in theory.
  - **`isDatabaseConnected()`** added (checks `mongoose.connection.readyState`) — the health endpoint (below) reads this rather than inspecting `mongoose.connection` directly.
  - **`GET /api/health` was reshaped** to the exact contract this step specified — moved *outside* the versioned `/api/{version}` namespace and the `{ success, data, error }` response envelope (unwrapped body, since infra/uptime monitors expect a small stable shape), and mounted in `app.ts` *before* the rate limiter so monitoring polling can't get throttled. Returns `{ status: "OK", database: "Connected", timestamp }` (200) when connected, `{ status: "ERROR", database: "Disconnected" }` (503) when not. The route file (`routes/health.routes.ts`) didn't move; only its mount point and controller body changed — `routes/index.ts` (the versioned mount point) no longer references it.
  - **Real bug found and fixed during verification, not a code defect**: the initial live-connect attempt failed 5/5 times with `ECONNREFUSED` on `querySrv` — Node's own DNS resolver (via this Windows machine's router-as-DNS-server setup) refused SRV-record queries specifically, even though `nslookup -type=SRV` against the same resolver succeeded outside Node. This is a documented Node-on-Windows + `mongodb+srv://` gotcha, not an Atlas or credentials problem. Fixed with `dns.setServers(['8.8.8.8', '1.1.1.1'])` at the top of `config/database.ts`, pointing Node's resolver at public DNS for SRV/TXT lookups without touching OS-level network config. Reconnected successfully on the very next attempt after the fix.
  - **Verified live**: `npm run dev` → logs `MongoDB connected — database "nalanda_tnpsc"` → `GET /api/health` returned `200 { "status": "OK", "database": "Connected", "timestamp": "..." }`. Graceful-shutdown logging (`SIGTERM`/`SIGINT` handlers in `server.ts`) could not be confirmed via this session's process-stop tooling on Windows (Windows doesn't natively deliver POSIX `SIGTERM`, so the harness's stop likely force-killed the process rather than triggering the handler) — the handler code itself is unchanged from Step 41 and should be re-verified with an actual `Ctrl+C`/`docker stop` once running interactively or in a real (Linux) deployment target, per docs/Architecture.md's containerized-backend assumption.
  - A local `backend/.env` now exists (gitignored, real Atlas URI + locally-generated real RS256 JWT key pairs + placeholder Firebase/Cloudinary values not yet needed by any code path) — this is the first time the backend has actually been run, not just built.
- [x] **2026-08-01 Production database models + seed system** (Sprint 3 Step 42) — 22 Mongoose models in `backend/src/models/`, covering every collection this step's build instruction named:
  - **Content taxonomy**: `Exam`, `Subject`, `Topic`, `Subtopic`, `Lesson`, `StudyMaterial`. **`Exam` is a real collection, not a bare enum** — a deliberate upgrade from `docs/Database.md` §4.1's plain string enum, since this step explicitly asked for a standalone `Exam` model; `code` stays a closed enum (`constants/exam.ts`, identical to `frontend/src/constants/exam.ts`'s 8 ids) so the extra collection buys admin-editable metadata (bilingual name, icon, ordering) without opening up arbitrary taxonomy growth. **`Video` (docs/Database.md §4.2) was merged into `Lesson`** — the requested model list named `Lesson` + `StudyMaterial`, not `Video`; `Lesson` is now the video-bearing teachable unit, `StudyMaterial` the notes/PDF sibling. `Subject`/`Topic`/`Subtopic` carry a `slug` field (new, not in `Database.md`) for URL-friendly lookups, matching the already-built frontend Learn module's slug-based routes.
  - **Question system**: `Question`, `QuestionAttempt`, `PracticeSession`, `Bookmark`. `Question` supports every field this step's instruction listed (bilingual text, image, embedded options with a Mongoose-layer "exactly one correct for mcq_single" validator, difficulty, a new `questionType` enum for future non-MCQ formats, explanation, source, PYQ indicator/year, a new `tnpscExamType` stage enum distinct from `examIds`, tags, active/premium/AI-explanation-eligible flags, soft delete). `PracticeSession.mode`/`Question.difficulty`/`.source` use the already-shipped frontend's kebab-case/snake_case values (`frontend/src/types/practice.ts`) rather than `docs/API.md`'s illustrative `topic_quiz`-style examples, since a real frontend contract now exists to match. **No `MockTest`/`LiveExam` models** — not in this step's requested list, `PracticeSession` stands alone with its own question-set snapshot for now.
  - **Student system**: `User` (identity/RBAC only — deliberately excludes any refresh-token/session storage per this step's "no auth yet" boundary; that needs its own design once the Auth module is built, since `docs/Authentication.md` implies multi-device sessions a single field can't represent), `Profile`, `StudyPlan`, `LearningProgress` (**new, not in `docs/Database.md` at all** — backs the already-built `frontend/src/services/learnProgressService.ts`, currently a localStorage mock; one document per user+subtopic, embedding per-lesson progress the same way `Question` embeds options).
  - **Engagement**: `CurrentAffair`, `Notification` (4 categories, matching the already-built frontend, not `docs/InformationArchitecture.md` §7.7's 5 — the same already-accepted deviation noted elsewhere in this doc), `Achievement`, `Leaderboard`. **No separate `Badge` catalog collection** — `constants/badges.ts` holds the ~5 badge codes as a code-level constant instead (same pattern already used for exam categories/roles/tiers), since an admin-editable badge catalog isn't needed until the Admin Panel (Phase 10) exists; `Achievement.badgeCode` references that enum directly.
  - **Commercial**: `Subscription`, `Payment` (immutable append-only log, `razorpaySignature` excluded from default reads via `select: false`).
  - **Analytics/AI**: `StudentAnalytics` (renamed from `docs/Database.md`'s plain `Analytics` to avoid a generic-name collision; adds `strongTopics` alongside `weakTopics` since `docs/Analytics.md`'s actual UI spec needs both at equal weight, even though `Database.md`'s original table only listed weak), `AIHistory` (tiered TTL implemented via a per-document `expiresAt` field set at write time, since a single TTL index can't vary its expiry window by document the way `docs/Database.md` §9.3 describes).
  - **Cross-cutting model design**: `models/shared/bilingualField()`/`bilingualParagraphsField()` (one reusable `{en, ta}` helper instead of duplicating bilingual sub-schemas 15+ times) and `models/shared/softDeletePlugin` (adds `deletedAt`, no automatic query-filtering middleware — deliberately explicit, not magic) are used consistently across every content model. Several `docs/Database.md` §4.1 `Map<string, number>` fields (`Profile.syllabusProgress`, `StudentAnalytics.sectionalScores`) became small bounded arrays instead — more idiomatic in Mongoose/TypeScript than a Map keyed by a stringified ObjectId, same underlying "denormalized summary" intent.
  - **A real MongoDB limitation was hit and fixed during verification**: `Question`'s originally-planned compound index on `{examIds, tags}` (both arrays) failed against the live Atlas cluster with `CannotIndexParallelArrays` — MongoDB rejects compound indexes across two multikey fields, a real constraint `docs/Database.md` §6's illustrative index table didn't account for. Fixed with two separate single-field indexes instead.
  - **Seed system** (`backend/src/seed/`, `npm run seed`) — idempotent (every document upserted on a natural key, safe to re-run; verified twice in a row with identical counts), seeding all 8 TNPSC exam categories plus a small 2-subject/4-topic/8-subtopic representative hierarchy (General Science → Physics/Biology, Aptitude & Mental Ability → Numerical Ability/Reasoning) with a lesson + study material per subtopic and 4 real, curated/PYQ questions total — deliberately not hundreds, per this step's explicit instruction. Subject/topic/subtopic names deliberately reuse strings already present in the frontend's mocks (e.g. "Newton's Laws of Motion") so a future session wiring the real frontend to this backend has matching content rather than a second, inconsistent taxonomy. Also added `npm run verify:seed` — a read-only script confirming all 22 collections exist in Atlas with documents in them and exercising one real populated read (`Question.findOne().populate(examIds/subjectId/topicId/subtopicId)`), not just that `npm run build` succeeds. **Location note**: seed scripts live in `backend/src/seed/`, not the repo-root `database/seed/` `docs/FolderStructure.md` §11 specifies — a deliberate, documented pragmatic deviation (the script needs direct access to the backend's own compiled models/config, and reuses its existing `tsx` toolchain).
  - **Verified against the real cluster, not just compiled**: `npm run typecheck`/`lint`/`format:check`/`build` all clean; `npm run seed` run twice against the live `nalanda_tnpsc` Atlas database with identical resulting counts (idempotency confirmed); `npm run verify:seed` confirmed all 22 collections physically exist with the expected document counts and a cross-collection populated read resolves correctly end-to-end.
  - **Still not built**: `repositories/`/`services/`/`validators/`/`controllers/`/`routes/` for any of these models, and all authentication — explicitly out of this step's scope ("do not implement authentication yet"). The next Sprint 3 step is almost certainly the Auth domain's first real vertical slice through those layers.
- [x] **2026-08-02 User/Profile/Onboarding/Dashboard backend domain module** (Sprint 3 Step 44, immediately following the Firebase Auth step) — the second real vertical slice, following the exact routes→controller→service→validator/repository layering the Auth module established. Full detail in `docs/PROJECT_CONTEXT.md` §14; summary:
  - **Schema**: `Profile.model.ts` gained `onboarding` (a dedicated sub-document — `completed`/`completedAt`/`examCategories`/`targetMonth`/`studyHoursBand`/`weakSubjects`/`learningStyle` — kept separate from `examGoals` since the wizard's shape doesn't map 1:1 onto that array's per-goal `Exam`-ref structure) and `theme`. New `constants/onboarding.ts` mirrors the frontend's ids exactly.
  - **New modules**: `users` (`GET/PATCH /users/me`, `PATCH /users/me/preferences`), `onboarding` (`GET /onboarding`, `PATCH /onboarding` for a per-step partial draft save, `POST /onboarding/complete` — also derives real `Profile.examGoals` by resolving each selected exam category to its seeded `Exam` document), `dashboard` (`GET /dashboard` — one combined response). `auth.service.ts`/`auth.controller.ts` extended (not rebuilt) so every session-establishing response and `GET /auth/me` now also carry `onboardingCompleted`.
  - **`GET /dashboard` is honest about data-source coverage** (this step's explicit "do not invent analytics or rank" instruction): `user`/`selectedExam`/`streak`/`dailyGoal`/`weakTopics` read real `User`/`Profile` data; `xp`/`coins`/`rank`/`weeklyProgress`/`continueLearning`/`recommendedTopics`/`achievements`/`currentAffairsPreview`/`upcomingExams`/`recentActivity` are honest zero/empty values — Learn/Practice/Analytics/Gamification/CurrentAffairs/LiveExams backend domains don't exist yet.
  - **Frontend**: new `services/userService.ts`; `services/onboardingService.ts` gained real `getOnboardingState`/`saveOnboardingDraft`/`completeOnboarding` alongside its still-mocked `generateStudyPlan`; `services/dashboardService.ts` now calls the real `GET /dashboard` behind an in-flight-request de-dupe cache (so the page's ~12 independently-mounting widgets collapse to one real network call); `use-onboarding-wizard.ts` now fetches/saves/completes against the real endpoints instead of `localStorage` alone (which remains as a same-tab fast path + offline fallback). Real, backend-sourced `AuthUser.onboardingCompleted` replaces the old `localStorage`-only `hasCompletedOnboarding()` check as the actual Onboarding-vs-Dashboard routing signal everywhere (`LoginPage`/`RegisterPage`/`GoogleLoginButton`/`OnboardingPage`/new `DashboardPage` guard); `AuthContext` gained `updateUser(patch)`.
  - **Endpoint paths deviate from `docs/API.md`'s illustrative §2/§11 shapes** (`/users/me` not `/profile`; one combined `/dashboard` not `/dashboard/summary` + `/dashboard/today-tasks`) — this step's build instruction specified the real paths directly; see `docs/PROJECT_CONTEXT.md` §13 for the full note.
  - **Verified against the real Atlas cluster, end to end**: both apps' `typecheck`/`lint`/`build` clean. A temporary, throwaway `tsx` script (written, run, then deleted) drove the actual running dev server over real HTTP through the full register→onboarding-draft(×3)→onboarding-complete→dashboard→auth/me→second-session(simulated logout+login) flow — 27/27 assertions passed against real MongoDB, then the test documents were deleted. **Real browser-driven Firebase login remains unverified** — `backend/.env`'s `FIREBASE_*` and `frontend/.env.local`'s `VITE_FIREBASE_*` are still literal placeholders, a pre-existing gap the Firebase Auth step already disclosed as out of its own scope, not something this step could fix; the manual test above bypassed only that one verification step (by calling `userSync.service.ts` directly with a hand-built decoded-token-shaped object), exercising every real layer downstream of it.
  - **No test runner exists for either app** — "run tests" for this step meant lint/typecheck/build plus the manual HTTP-level verification above.
- [x] **2026-08-02 Real Learning module** (Sprint 3 Step 45, immediately following Step 44) — the third real backend vertical slice, and the first to fully retire a previously-mocked frontend feature rather than build a new one. Full detail in `docs/PROJECT_CONTEXT.md` §14; summary:
  - **No new models** — reused `Exam`/`Subject`/`Topic`/`Subtopic`/`Lesson`/`StudyMaterial`/`LearningProgress`/`Bookmark` from the Step 42 models pass exactly as designed, per this step's explicit "don't create duplicate models" instruction.
  - **New modules**: `exams`, `subjects` (real exam-based filtering — defaults to the signed-in user's own primary Onboarding exam goal when unspecified; nested topics/subtopics/lesson-detail/lessons/video/notes), `lessons`/`study-materials` (standalone by-id lookups), `learn` (search, Practice's subtopic-location cross-module resolver), `learning-progress` (get/update, with a `POST /bookmarks/toggle`-style create-or-remove-in-one-call design for bookmarks too). `subjectId`/`topicId`/`subtopicId` stay real Mongo `slug`s throughout, never raw ObjectIds — every existing `ROUTES.learn*` URL kept working unchanged.
  - **Subject/topic completion percentages are computed live from `LearningProgress`, never a stored duplicate** — this step's explicit instruction. Video auto-completes its subtopic at ≥90% watched, matching the old frontend mock's threshold exactly.
  - **Frontend**: `services/learnService.ts` is now fully real (no longer a facade over a mock); `services/learnProgressService.ts` is a hybrid (real subtopic progress/video-note bookmarks, still-mocked Practice-question/Current-Affairs bookmarks). `services/mock/subjectsMockService.ts`/`topicsMockService.ts` deleted once unused. One small, deliberately-scoped cross-module touch outside Learn itself: Practice's `mode-config-form.tsx` adapted its existing "Practice This Topic" pre-fill to the now-async `findSubtopicLocation` — not a Smart Practice feature change.
  - **Seed data expanded from 2 to 4 subjects** (added Tamil, Indian Polity) — final: 4 subjects/6 topics/12 subtopics/12 lessons/12 study materials/5 questions, still deliberately small.
  - **A real MongoDB/BSON bug was found and fixed during verification**: `learningProgressRepository.findByUser` was spreading an explicit `subjectId: undefined` into a Mongo filter — the Node driver serializes JS `undefined` as BSON `null` (not "key absent"), so the "give me all my progress" query silently matched zero documents. Fixed by only including the key when a real value is present.
  - **Verified against the real Atlas cluster, end to end**: both apps' `typecheck`/`lint`/`build` clean. A temporary, throwaway `tsx` script (written, run, deleted) drove the actual running dev server through the full hierarchy → progress (record-visit → watch-to-95%-auto-completes → subject/topic percentages recompute correctly) → bookmark (toggle on/off, real title/breadcrumb resolution) → search → Practice handoff → a second, brand-new session for the same user still seeing identical state. All assertions passed after the BSON-filter fix; test documents deleted, shared seed content untouched.
  - **Not verified**: an actual browser click-through — real Firebase login now works (Step 44's credentials are real as of this same day), so this is independently browser-testable; recommended before treating the click-through UX as fully proven, not just the API contracts.
  - **Not started, as instructed**: Smart Practice itself. The Revision queue also stays fully mocked (no backend model exists for it, out of scope) but now reads real video/note bookmarks through the updated hybrid `getBookmarks()`.
- [ ] `database/seed/` (repo-root, per `docs/FolderStructure.md` §11) — superseded in practice by `backend/src/seed/` above; the repo-root folder remains unused.
- [x] **2026-07-31 Mock Service Layer Consolidation** (`frontend/src/services/mock/`) — not backend work itself, but the frontend-side readiness for it: every `frontend/src/services/*.ts` file is now a stable facade delegating to a same-named file in `services/mock/`, including the three that previously made real (always-failing) `apiClient` calls (`publicService.ts`, `paymentsService.ts`, `currentAffairsService.ts`'s `getCurrentAffairsPreview`) — the whole frontend now runs fully independent of a backend, and Sprint 3 becomes a pure swap-the-facade-body exercise, file by file, with zero consumer-side changes needed. Also added a genuinely new module with no prior frontend code at all: Leaderboard (`types/leaderboard.ts`, `services/leaderboardService.ts`, `services/mock/leaderboardMockService.ts` — top-100 entries + "my position," now the single source of truth Dashboard's `getRankSummary` and Analytics' rank figures both derive from instead of independently hardcoding the same numbers). See `docs/PROJECT_CONTEXT.md` §14 ("Mock Service Layer Consolidation") for full detail, including a real pre-existing `StatCard` rendering bug found and fixed during verification (a `trendLabel` was silently never rendering without an accompanying `trend` number) and the deliberate, flagged reversal of the Landing Page's previous "real endpoint, honest ErrorState" demonstration for its three now-mocked public sections.

## Phase 6 — Public Website Pages 🟡

- [x] **Landing Page** (`Landing_Page_Design.md`) — all 11 sections: Hero (animated background, floating AI icons, AI orb, mouse parallax), Live Stats Counters (count-up-on-scroll), Feature Cards, Current Affairs Preview, Top Rankers, Success Stories, Pricing, Testimonials (auto-scroll marquee + required Pause/Play control), FAQ. The six data-driven sections read via React Query from `services/publicService.ts`/`paymentsService.ts`/`currentAffairsService.ts` (`docs/API.md` §17 endpoint shapes preserved as the facade's documented target). **Updated 2026-07-31**: these three services were originally wired to real (but always-failing, since no backend exists) `apiClient` calls specifically to demonstrate a correct loading-skeleton→error-state sequence; the Mock Service Layer Consolidation (Phase 5 entry above) converted them to mocked, realistic data instead, per that session's explicit "function exactly as if a backend exists" instruction — a deliberate, flagged reversal of this original behavior, not a regression.
- [x] `layouts/website-layout.tsx` — built here, wraps Navbar/`AnimatedOutlet` (page-to-page cross-fade)/Footer.
- [ ] Pricing (standalone page — today Pricing is only a Landing Page section), Exams landing (`/exams/:slug`), About, Contact, Blog index — none started.

## Phase 7 — Student Dashboard Core 🟡

- [x] **2026-07-31 Dashboard home screen** (`pages/dashboard/dashboard-page.tsx`, `features/dashboard/`), per the new `docs/Dashboard.md` (written this session — no such spec existed before). 19 mocked widgets: Welcome Banner, Daily Study Goal, Today's Tasks, Study Streak/XP/Coins/Rank, Weekly Progress, Continue Learning, Recommended Topics, Weak Subjects, Current Affairs Summary, Upcoming Exams, Quick Practice, AI Mentor Card, Notifications, Recent Activity, Achievements, Premium Banner — see `PROJECT_CONTEXT.md` §14 for full detail. Replaces the Phase-4 placeholder at `/app/dashboard`. **2026-08-02 update (Step 44, Phase 5 entry above)**: Welcome Banner/Daily Study Goal/StatsRow's Streak+XP+Coins/Rank/Weekly Progress/Continue Learning/Recommended Topics/Weak Subjects/Achievements/Current Affairs Summary/Upcoming Exams/Recent Activity now read the real `GET /dashboard` (honest zero/empty states where no backend module exists yet — see Phase 5); Today's Tasks/Notifications stay mocked (no Study Plan/Notifications backend module). The page also gained a real `onboardingCompleted`-based redirect guard, and the Rank tile gained a small honest "not enough data" empty state.
- [x] **2026-07-31 Learn module** (`pages/learn/`, `features/learn/`, `layouts/learn-layout.tsx`), per `docs/Learn_Module.md`. Full hierarchy — Subjects → Topics → Subtopics → Lesson Details → Video/PDF — plus Bookmarks and a spaced-repetition Revision queue, all at real slug-based routes (`ROUTES.learn*`, `constants/routes.ts`) nested under `LearnLayout`'s cross-fading `AnimatedOutlet`. Search (subject/topic/subtopic jump, debounced/grouped), filter chips at every list level, per-subject/topic progress + subtopic completion, a module-local "Continue Learning" banner, and a fully mocked "video player" (`features/learn/hooks/use-mock-video-player.ts` — simulated playback, no real media backend yet) with auto-watched-at-90%. Browser-verified end-to-end via a temporary Playwright script (installed and removed per-session, same precedent as earlier phases) — one real bug caught and fixed this way: `LearnLayout`'s header double-wrapped the already-self-linking `Logo` component in a second `<Link>`, producing invalid nested `<a>` tags. **2026-08-02 update (Step 45, Phase 5 entry above)**: `services/learnService.ts` is now fully real (Subjects/Topics/Subtopics/Lesson Details/Video/Notes/Search all call the live backend, exam-filtered by default) — no longer a mock at all, and `services/mock/subjectsMockService.ts`/`topicsMockService.ts` (the old static content tree) were deleted. `services/learnProgressService.ts` is now a hybrid: subtopic completion, video-watch progress, and video/note bookmarks are real (MongoDB); Practice question bookmarks and Current Affairs bookmarks stay mocked, since neither module has a backend yet. The Revision queue itself remains fully mocked (no backend model exists for it) but now surfaces real video/note bookmarks through the updated `getBookmarks()`. The video player is still a full simulation (no real media backend, unchanged).
- [x] **2026-07-31 Smart Practice engine** (`pages/practice/`, `features/practice/`, `layouts/practice-layout.tsx`), per `docs/Smart_Practice.md`. Five modes (Topic Quiz, 100 Questions, Sectional, Mock, PYQ) sharing one session-taking UI, at real routes (`ROUTES.practice*`) nested under `PracticeLayout`. Preserves the doc's core mode split: Topic Quiz/100 Questions get a soft (count-up) timer and immediate per-question reveal + AI Explanation once answered, reusing `components/question-card.tsx`'s existing reveal props; Sectional/Mock/PYQ get a hard (countdown, color-escalating, auto-submitting) timer and defer all reveal to post-submission Review — the same question palette component recolors from answered/marked/unanswered (live) to correct/incorrect/skipped (Review) depending which mode it's used in. "100 Questions" is honestly scoped: rather than padding the mock question bank to a literal 100 with repeats, a session simply uses every question in the (~24-item, hand-authored, real-content) bank, and the progress tracker always shows the true count. AI Explanation is an explicit **placeholder** (per this session's scope) — real mock explanation text, but "Explain Again"/"Ask a Follow-up" `toast.info(...)` a future-phase stub, the same precedent Learn's Video/Notes screens set. Question bookmarks reuse `services/learnProgressService.ts`'s existing bookmark store directly (`contentType: 'question'`) rather than a parallel system, which also means the Bookmarks page's Questions tab is now genuinely populated. XP/Coins/5 named Achievements (`services/practiceSessionService.ts`, `localStorage['nalanda-practice-sessions'|'nalanda-practice-results'|'nalanda-practice-progress']`) are Practice's own persisted ledger — deliberately not wired into Dashboard's separate static mock XP/Coins numbers, a known scope boundary (rewiring Dashboard's mock is out of scope here). Dashboard's `QuickPracticeCard` and Learn's "Practice This Topic" handoff (`?subtopicId=`, previously dead links, docs/Learn_Module.md §6) both now resolve into a real, pre-filtered flow — closing the loop that doc explicitly called "connective tissue." Browser-verified end-to-end via a temporary Playwright script (installed and removed per-session) — one real bug caught this way: the Summary/Review pages were reusing the Session page's pre-submission React Query cache entry (`lib/query-client.ts`'s 1-minute `staleTime`) instead of the freshly submitted session, fixed by invalidating that query key on successful submit.
- [x] **2026-08-03 Real Smart Practice backend — Topic Quiz mode** (Sprint 3 Step 46, immediately following Step 45) — the fourth real backend vertical slice. Full detail in `docs/PROJECT_CONTEXT.md` §14; summary:
  - **Scope boundary, deliberate**: only `mode: 'quiz'` (Topic Quiz — Select Exam → Subject → Topic → Configure → Start, this step's exact described flow) is now MongoDB-backed. 100 Questions/Sectional/Mock/PYQ have no backend module and stay entirely on the original `localStorage` mock (`services/practiceSessionService.ts`'s `createMockSession`/`finishMockSession`/etc.) — untouched, unredesigned.
  - **No new models** — reused `Question`/`PracticeSession`/`QuestionAttempt`/`Bookmark` exactly as designed in Step 42, per this step's explicit "don't duplicate models" instruction. `Bookmark.contentType`'s existing `'question'` enum value (declared but unwired since Step 42) is now genuinely used for the first time.
  - **New backend module**: `practice` (`repositories/{question,practiceSession,questionAttempt}.repository.ts`, `services/practice.service.ts`, `controllers/practice.controller.ts`, `validators/practice.validator.ts`, `routes/practice.routes.ts`, mounted at `/practice`) — `POST /practice/sessions` (random `$sample`-based question selection, never the full eligible pool loaded client-side, scoped to stay efficient once a topic holds 100+ questions), `GET /practice/sessions/:id` (resume), `POST /practice/sessions/:id/answers` (immediate correctness + explanation reveal, writes both the session's embedded `answers[]` and an immutable `QuestionAttempt` row), `POST /practice/sessions/:id/finish` (idempotent scoring), `GET /practice/sessions/:id/result`, `GET /practice/sessions/:id/review` (full revealed answer key + AI Explanation entitlement + bookmark status per question, only once submitted), `GET /practice/history` (paginated, subject/topic-filterable, date-sortable). Endpoint shapes follow this step's own explicit build instruction directly, not `docs/API.md` §6's illustrative `PUT .../answers/{questionId}` + `POST .../submit` — see `docs/PROJECT_CONTEXT.md` §13 for the note.
  - **`bookmark.service.ts`/`bookmark.validator.ts` extended** (not rebuilt) to a discriminated-union request body — `{contentType: 'question', questionId}` alongside the existing `{contentType: 'lesson'|'study_material', subtopicSlug}` — since a Question has no slug of its own, unlike Subtopic-owned content. `LearnBookmarkDTO` gained a `contentId` field (the raw target document id) so the frontend can match "is this exact question bookmarked."
  - **AI Explanation entitlement, not a live AI call** (this step's explicit boundary — Sprint 4's job): `GET .../review` returns `aiExplanation: {eligible, entitled}` per question, `entitled` derived from `req.user.subscriptionTier !== 'free'` (the same independent-tier-claim JWT check every other premium gate in this codebase uses). Frontend: free users see the existing `PremiumCard` lock treatment (reused, not reinvented) wrapping an AI Explanation teaser, routing "Upgrade" to the existing `ROUTES.subscription`; Plus/Pro/Institutional users see an enabled AI Explanation card with a disclosed "coming in the next implementation phase" state — no fake AI response text, per this step's explicit "do not fake AI responses" instruction. Standard Explanation (always available, every tier) is now visually distinct from the AI Explanation section, not the same card.
  - **Security** (this step's explicit checklist): every route behind `authenticate`; every session/answer/finish/result/review lookup scoped to `{_id, userId}` (never a bare `findById`); unanswered-question and cross-session answer attempts rejected (400); answering an already-submitted session rejected (409, immutability); a per-minute rate limiter on the answer-submission route; `sessionId`/`questionId` Zod-validated as real ObjectIds before any query; sanitized question DTOs never include `isCorrect` or the answer key pre-submission.
  - **Frontend**: `services/practiceSessionService.ts` now routes every function by id shape — a 24-hex-char Mongo ObjectId means the real API, anything else (the mock bank's `q-...` ids, `generateId('session')` ids) stays on the original mock path, so `100 Questions`/`Sectional`/`Mock`/`PYQ` needed zero UI changes. `PracticeQuestion`'s `correctOptionId`/`explanation`/`hint` became optional (never sent pre-answer by the real backend); the reveal now lives on `PracticeAnswer.{isCorrect,correctOptionId,explanation}` instead, populated either by the real answer-submission response or (for mock sessions) computed locally — `components/question-card.tsx`/`AiExplanationPanel` read `answer.correctOptionId ?? question.correctOptionId` uniformly, so both paths render identically. `features/practice/components/mode-config-form.tsx` gained the Number-of-Questions (5/10/20/25) and Difficulty (All/Easy/Medium/Hard) `ToggleChip` rows this step's config screen required, shown only for Topic Quiz; Subject/Topic became required (not optional) for that mode, matching the real backend's requirement. New `pages/practice/practice-history-page.tsx` (`ROUTES.practiceHistory`, `/app/practice/history`) — paginated list of completed real sessions, linked from the Summary page alongside a new "Go to Analytics" button (Step 46 §12's Results-page checklist). `PracticeSummaryCard`'s XP/Coins now honestly show `+0` for real sessions (no gamification backend exists yet) rather than a fabricated number.
  - **Dev seed expanded from 5 to 29 questions** (`backend/src/seed/data/syllabus.seed.ts`) — spread across all 6 existing topics/12 subtopics, easy/medium/hard mixed, `general-science/physics` alone holding exactly 10 (5 in Newton's Laws + 5 in Units and Measurements) specifically so a real "10 Questions" Topic Quiz has enough real content to complete end-to-end. Still deliberately small — not a production content seed.
  - **Verified against the real Atlas cluster, end to end**: backend `typecheck`/`lint`/`build` clean; frontend `typecheck`/`lint`/`build` clean. A temporary, throwaway `tsx` script (written, run, then deleted, same precedent as Steps 44/45) drove the actual running dev server over real HTTP — 42/42 assertions passed: session creation (sanitized, no answer leakage), resume, cross-user ownership rejection on both session-read and answer-submit, a mixed correct/incorrect/skipped 10-question run, finish (idempotent, immutable-after-finish enforced with a 409), result reload, review (free-tier `entitled: false`, Plus-tier `entitled: true`, difficulty filter respected), bookmark create/duplicate-idempotent/list/toggle-off, and paginated history — then all test documents (2 users, sessions, attempts, bookmarks) were deleted, shared seed content untouched. **Real browser-driven login was not exercised this session** (no Firebase test credentials available to the agent) — backend/frontend dev servers were left running and a precise manual test script (Login → Smart Practice → 10 Questions → answer with intentional mistakes → verify Standard/AI Explanation entitlement → finish → Results → Review → History → MongoDB check → refresh/logout/login persistence) was handed off for the user to run, per this step's own "give me a manual browser test" instruction.
  - **Not implemented, as instructed**: Analytics, any AI provider integration (OpenAI/Claude/Gemini), and a real backend Hints module (the existing free-tier daily-hint-cap UI stays on its pre-existing `localStorage` mock, unchanged — Step 46's build instruction never mentioned Hints).
- [x] **2026-08-03 Real Weekly Live Exam** (Sprint 3 Step 48, immediately following Step 46) — the fifth real backend vertical slice, and the first entirely new module built directly against the real backend from day one (`features/live-exams/` was an empty scaffold; no prior mock existed to swap out). Full detail in `docs/PROJECT_CONTEXT.md` §14; summary:
  - **Two new models**: `LiveExam` (self-contained by design — title/description/examId/subjectIds/questionIds/schedule/duration/totalMarks/marksPerQuestion/negativeMarking/instructions/resultPublication/status, deliberately not referencing a `MockTest` model since none exists in this codebase and this step didn't ask for one — a documented divergence from `docs/Database.md` §4.3's illustrative `Live Exams` shape, same "real build instruction overrides illustrative doc shape" precedent this project has set repeatedly) and `LiveExamAttempt` (its own collection per this step's explicit "store separately from ordinary Smart Practice" instruction — never `PracticeSession`/`QuestionAttempt`), both registered in `backend/src/models/index.ts` and `seed/run.ts`'s `ALL_MODELS`.
  - **New backend module**: `liveExams` (`repositories/{liveExam,liveExamAttempt}.repository.ts`, `services/liveExam.service.ts`, `controllers/liveExam.controller.ts`, `validators/liveExam.validator.ts`, `routes/liveExam.routes.ts`, mounted at `/live-exams`) — `GET /live-exams?tab=upcoming|live|completed`, `GET /live-exams/attempts` (My Attempts — a static route registered before `/:liveExamId` for the same Express-declaration-order reason Practice's history route needed), `GET /live-exams/:id`, `POST /live-exams/:id/join` (idempotent get-or-create, doubles as "Start"), `GET /live-exams/:id/attempt` (resume), `POST /live-exams/:id/attempt/answers` (never reveals correctness — Live Exam has no mid-exam reveal at all, stricter than Practice's Topic Quiz), `POST /live-exams/:id/attempt/finish`, `GET /live-exams/:id/result` (publication-gated).
  - **`status` (draft/scheduled/live/completed/cancelled) vs. "effective status"**: the stored `status` field only expresses human overrides (`draft` = never shown to students, `cancelled` = explicitly called off); Upcoming/Live/Completed is always a fresh `computeEffectiveStatus(exam, serverNow)` comparison against `scheduledStartAt`/`scheduledEndAt` — never the stored field alone — satisfying this step's explicit "use server time as authoritative for exam eligibility and timing" rule without needing a cron job to keep `status` in sync.
  - **Server-time-authoritative auto-submit without a background job**: a student's personal deadline is `min(startedAt + durationMinutes, exam.scheduledEndAt)`; every attempt-touching service function calls a lazy `ensureFinalized()` first, which auto-scores and submits (`submissionType: 'auto'`) any `in-progress` attempt whose deadline has silently passed (tab closed, crash, timer never fired client-side) — the frontend's own `QuestionTimer`-driven submit is the primary path, this is the safety net. Verified directly: a smoke-test attempt was back-dated past its deadline via a direct Mongoose write, then a normal `GET .../attempt` call auto-finalized it.
  - **Answer security**: sanitized question DTOs (reused from `practice.service.ts`'s `sanitizeQuestion`/`explanationText`, now exported for cross-module reuse) never carry `correctOptionId`; every session/answer/finish/result lookup is scoped to `{userId, liveExamId}` (never a bare id lookup); answering a finished attempt returns `409`; a per-minute rate limiter guards the answer-submission route; a real DB-level `{userId, liveExamId}` unique index prevents duplicate attempts (not just a service-layer check) — verified by calling `join` twice and confirming exactly one `LiveExamAttempt` document exists.
  - **Result publication is genuinely gated, not just delayed client-side**: `GET .../result` returns `{available: false, publishAt}` until `now >= scheduledEndAt` AND (`resultPublication.mode === 'immediate'` OR `now >= resultPublication.publishAt`) — verified that a student who already finished a still-live exam gets `available: false` even though their own score is fully computed and stored server-side, closing the "share your score with peers still taking it" leak this step's requirement implies.
  - **Entitlement**: joining requires `subscriptionTier !== 'free'` (`docs/InformationArchitecture.md` §9's access matrix — Live Exams: Free gets "Schedule preview" only, Plus gets "Full") — browsing all four tabs stays free-tier visible, only `POST .../join` 403s, matching the existing `PremiumCard` lock pattern (reused verbatim, not reinvented) already established for Practice's AI Explanation (Step 46) and Learn's premium notes.
  - **No real-money prizes** — out of scope per this step's explicit instruction; nothing prize/wallet/payout-shaped was added anywhere in the model or UI.
  - **Frontend**: brand-new `features/live-exams/` (no mock precedent to swap, unlike every other Sprint 3 step) — `Countdown` (server-time-synced, one-time clock-skew offset then a local 1s tick), `LiveExamCard`, `MyAttemptCard`, `LiveExamQuestionPanel` (deliberately simpler than Practice's — no hint, no reveal, no AI Explanation; `QuestionCard` never receives `correctOptionId` mid-exam, so the leakage rule is enforced structurally on the frontend too, not just trusted from the backend). Four new routes under a new `layouts/live-exams-layout.tsx` (mirroring `PracticeLayout`): `/app/live-exams/:tab` (Upcoming/Live/Completed/My Attempts — one `ToggleChip` row, the same tab-switch pattern `CurrentAffairsPage` already set for Daily/Weekly/Monthly), `/app/live-exams/exam/:liveExamId` (detail — schedule, instructions, marking scheme, countdown, Start/Resume/View Result), `.../session` (reuses Practice's `QuestionTimer`/`QuestionPalette` directly — Live Exams deliberately share Practice's test-taking mechanics per `docs/InformationArchitecture.md` §7.10 while staying a distinct module identity), `.../result` (publication-gated, shows a live countdown to `publishAt` when not yet available). `ROUTES.liveExams` was repurposed from a dead `(examInstanceId) => .../live-exams/${id}` (no page ever existed behind it) to the tab-list builder, with a new `liveExamDetail`/`liveExamSession`/`liveExamResult` set for the rest — the one call site that used the old shape (`features/dashboard/components/upcoming-exams-card.tsx`) was updated to match, closing a previously dead Dashboard link.
  - **Dashboard's Upcoming Exams widget is now real** — `backend/src/services/dashboard.service.ts`'s `GET /dashboard` `upcomingExams` field, previously an honest empty array (no Live Exams backend existed when Step 44 built it), now queries the real top-3 upcoming `LiveExam`s (exam-goal-filtered when the student has one); the frontend widget (`features/dashboard/components/upcoming-exams-card.tsx`) needed zero changes since it already read this field.
  - **Dev seed**: 6 representative Live Exams (`backend/src/seed/data/liveExams.seed.ts`) scheduled **relative to seed-run time** (not hardcoded dates, so they stay Upcoming/Live/Completed correctly no matter when the seed is actually run) — one currently live at seed time (so the full join→answer→submit flow is immediately testable), one completed with immediate publication, one cancelled (proving the "never silently disappear a scheduled event" completed-tab behavior), one scheduled-publication example (results a day after close), one further-out upcoming, and one `draft` (proving drafts never reach a student). Question sets are assembled from the existing 29 syllabus questions by tag — no duplicate question content authored.
  - **Verified against the real Atlas cluster, end to end**: backend `typecheck`/`lint`/`build` clean; frontend `typecheck`/`lint`/`build` clean. Two temporary, throwaway `tsx` scripts (written, run, then deleted, same precedent as every prior real-backend step) drove the actual running dev server over real HTTP — 56 assertions on the full flow (tab filtering incl. cancelled/draft handling, detail, free-tier 403, join, duplicate-join idempotency, resume, cross-student ownership rejection on both read and write, answering incl. mark-for-review-only, finish idempotency, post-finish write rejection, publication-gated result, My Attempts, cannot-join-ended/cancelled, lazy auto-finalize, malformed-id validation) plus 6 more re-verifying the result DTO's new `serverTime` field — all passed, test data deleted after each run. **Real browser-driven login was not exercised this session** (no Firebase credentials available to the agent, same disclosed gap as Step 46) — both dev servers were left running and a manual test script was handed to the user instead.
- [x] **2026-07-31 Current Affairs module** (`pages/current-affairs/`, `features/current-affairs/`, `layouts/current-affairs-layout.tsx`), per `InformationArchitecture.md` §7.9 (no dedicated design doc — see `PROJECT_CONTEXT.md` §13). Daily/Weekly Digest feeds plus a browsable Monthly Archive, all at real routes (`ROUTES.currentAffairs*`) nested under `CurrentAffairsLayout`. Category filter (8 categories), debounced search, bookmarking (reuses `learnProgressService.ts`'s store directly, `contentType: 'current-affairs'` — populates the Bookmarks page's previously-empty "Current Affairs" tab for the first time), an "Important Highlights" callout per article, a per-article 2-3-question quiz (self-contained dialog reusing `QuestionCard`, not routed through Practice's session engine), a Web Share API share button with clipboard fallback, and scroll-tracked read progress with 90%-auto-complete. Two mock services: `currentAffairsService.ts` (extended in place — its pre-existing real `getCurrentAffairsPreview` untouched, every full-module read added as mocked against a 19-article hand-authored dataset spanning May-July 2026) and the new `currentAffairsProgressService.ts` (read-progress only, `localStorage['nalanda-current-affairs-progress']`). Browser-verified end-to-end via a temporary Playwright script (installed and removed per-session) — zero console errors, no fixes needed.
- [x] **2026-07-31 Analytics module** (`pages/analytics/analytics-page.tsx`, `features/analytics/`) — a single "professional dashboard" page at `/app/analytics` (`ROUTES.analytics`, simplified from the illustrative per-view route split), not `docs/Analytics.md`'s full 14-view drill-down hierarchy (this session's build instruction specified 9 named charts directly). A 4-tile summary row (`AnalyticsSummaryRow`) plus 9 real Recharts charts: Subject Accuracy, Weekly Study Time, Monthly Progress, Weak Areas (with a "Study this topic" action row), Strong Areas, Practice History, Speed Analysis (You vs. cohort benchmark), Rank Prediction (generated bell curve + `ReferenceLine`), and Goal Completion (`RadialBarChart`). First real feature to use the previously-dormant `components/charts/chart-container.tsx` (defined in Phase 2, unused until now). One mocked read-only service, `services/analyticsService.ts` — no stateful sibling, since Analytics has no user-mutable state. Rank Prediction reuses `dashboardService.ts`'s existing rank-summary mock numbers for cross-module consistency. `features/dashboard/components/weekly-progress-card.tsx`'s dead "View full Analytics" link now resolves here. Verified via `lint`/`typecheck`/`format:check`/`build` only — **not** browser-verified with Playwright this session (scope was explicitly "run lint and build"), unlike the Learn/Practice/Current-Affairs precedent; a future session should still give it a click-through pass.
- [x] **2026-08-03 Real Individual Student Analytics backend** (Sprint 3 Step 47, immediately following Step 48) — the sixth real backend vertical slice, and the first to make every function in an already-real-UI service file (`services/analyticsService.ts`) call a live endpoint instead of `services/mock/`. Full detail in `docs/PROJECT_CONTEXT.md` §14; summary:
  - **No new models, no background job** — every number is computed live via MongoDB aggregation from `QuestionAttempt`/`PracticeSession`/`LearningProgress` (this step's explicit source list), never the dormant `StudentAnalytics` materialized-view collection (modeled since Step 42, still unused — a background-job scheduler doesn't exist, `docs/Database.md` §8.1's "materialize, never compute live" rule is a documented, deliberate exception at this data volume, revisit once one exists).
  - **New backend module**: `analytics` (`repositories/analytics.repository.ts` — pure aggregation pipelines against the high-volume `QuestionAttempt` collection, `$lookup`-joined to `Question` for subject/topic/difficulty, plus one lean bulk read of the user's own low-volume `PracticeSession` documents for session-level stats — never a bulk read of `QuestionAttempt` itself, this step's explicit "avoid loading every attempt into memory" rule; `services/analytics.service.ts`, `controllers/analytics.controller.ts`, `validators/analytics.validator.ts`, `routes/analytics.routes.ts`, mounted at `/analytics`) — `GET /analytics/{overview,subjects,topics,weak-areas,strong-areas,subject-accuracy,weekly-study-time,monthly-progress,practice-history,speed-analysis,rank,goal-completion,difficulty}`.
  - **Transparent, rule-based classification (never AI, this step's explicit requirement)**: a subject/topic needs ≥5 answered questions before it's labeled at all (below that: `not_enough_data`, never a misleadingly precise verdict); above that, accuracy ≥80% → `strong`, ≥65% → `good`, ≥50% → `needs_improvement`, else `weak`. Trend direction (`improving`/`declining`/`steady`) compares the recent half of a subject/topic's attempt history against its earlier half (needs ≥6 attempts). Weak-area **priority score** (documented, deterministic): starts at `100 − accuracy`, +15 if practiced in the last 7 days / +7 if last 30 (recent poor performance ranks higher), +10 if average response time is ≥20% above the student's own overall average (slow *and* inaccurate ranks highest) — closing docs/Analytics.md §6/§8's "prioritize by low accuracy, recency, and speed" requirement with a formula, not a model call.
  - **Cohort-wide numbers (percentile/rank, the Speed benchmark) are real, not fabricated** — computed by aggregating every user's `QuestionAttempt`s (a genuine "how does this student compare to everyone else who's practiced" query), disclosed with `isSmallCohort` (cohort < 20) rather than presented with false confidence, per `docs/Analytics.md` §6/§11's existing small-cohort-honesty rule (now wired into `RankPredictionChart`, a small additive change, not a redesign).
  - **`services/analyticsService.ts` is now real** — every function calls the live backend; `services/mock/analyticsMockService.ts` was deleted once nothing imported it anymore, same precedent as the Learn content-tree mocks (Step 45). Every existing chart component/type shape was kept **unchanged** (this step's explicit "connect existing tab to real APIs, do not redesign" instruction) — the facade functions map real DTOs onto the exact same `types/analytics.ts` contracts (`AnalyticsSummary` gained one additive `hasActivity: boolean` field; `RankPredictionData` gained one additive optional `isSmallCohort?: boolean`, both backward-compatible). One new, purely additive chart, `QuestionDifficultyChart` (Easy/Medium/Hard accuracy), added to the page in the same `ChartContainer`/Recharts style as every other chart — not a redesign of anything existing, closing this step's §8 requirement.
  - **New-user empty state** (§10's explicit requirement): `AnalyticsPage` now gates the entire chart grid behind `overview.hasActivity` — a student with zero `QuestionAttempt`s sees one honest `EmptyState` ("Complete practice questions to unlock your performance analytics," a "Start Practice" CTA to `ROUTES.practice('quiz')`) instead of any chart, never a zeroed/fake-looking chart grid.
  - **Verified against the real Atlas cluster, end to end**: backend `typecheck`/`lint`/`build` and frontend `typecheck`/`lint`/`build` all clean. A temporary, throwaway `tsx` script (written, run, then deleted, same precedent as every prior real-backend step) bypassed Firebase the same way Steps 44/45/46/48 did (calling `userSync.service` directly with a hand-built decoded-token-shaped object) and drove the real `practice.service`/`analytics.service` functions directly against live MongoDB — 2 subjects, 3 topics (`physics`/`biology` under General Science, `numerical-ability` under Aptitude & Mental Ability), 6 real Topic Quiz sessions with deliberately varied correctness (to exercise all four classification bands), plus 2 direct-write backdated `QuestionAttempt`s to exercise the streak calculation across days, plus 2 subtopics marked complete for the Goal Completion check. 26/28 assertions passed against real MongoDB, cross-checked independently (a second, deliberately naive raw-Mongo accuracy computation matched the service layer's aggregation exactly); the 2 "failures" were test-script math errors, not code bugs (an expected-value miscalculation in the test itself, and an assertion that a sub-second scripted session would have nonzero elapsed minutes) — documented as such, not silently ignored. Test data (user, profile, sessions, attempts, progress) fully deleted after the run; shared seed content untouched.
  - **Not implemented, as instructed**: any predictive AI, and the remaining 14-view drill-down below.
- [x] **2026-08-03 Real Current Affairs + Leaderboard backend** (Sprint 3, immediately following Step 47) — the seventh and eighth real backend vertical slices, landed together. Full detail in `docs/PROJECT_CONTEXT.md` §14; summary:
  - **`CurrentAffair.model.ts` extended** (no new collection) — `tags: string[]` (general topical tags, distinct from the existing exam-syllabus-mapped `examRelevanceTags`), `imageUrl`/`imageAlt` (optional image metadata — schema-real, unset in dev seed since no real images exist to attach, never a fabricated placeholder URL), and a new embedded `quizQuestions[]` (the article's self-contained, immediately-answer-revealing comprehension quiz — deliberately **not** a `Question` reference, since it's a genuinely different leakage model than the graded TNPSC syllabus bank; the pre-existing `quizQuestionIds` field now really is used, for "related questions" — real syllabus `Question` links, sanitized like every other Practice question DTO).
  - **New backend module**: `currentAffairs` (`repositories/currentAffair.repository.ts`, `services/currentAffair.service.ts`, `controllers/`, `validators/`, `routes/currentAffairs.routes.ts`, mounted at `/current-affairs`, `optionalAuthenticate` throughout — every read is public per `docs/API.md` §13, an authenticated caller additionally gets `isBookmarked`) — `GET /current-affairs/preview` (public Landing Page/Dashboard teaser), `GET /current-affairs` (period/category/month/pagination — Today = `daily`, This Week = `weekly`, exactly matching the already-shipped frontend's existing period tabs, no new UI concept invented), `GET /current-affairs/:id`, `GET /current-affairs/:id/quiz`, `GET /current-affairs/:id/related-questions` (new), `GET /current-affairs/search`, `GET /current-affairs/archive/months`, `GET /current-affairs/archive/:month`.
  - **Current Affairs bookmarks are real** — `bookmark.service.ts`/`bookmark.validator.ts` extended with a `current_affairs` discriminated-union variant (direct `contentId` reference, like `question`, since a `CurrentAffair` has no subtopic slug to resolve through); `LearnBookmarkDTO`'s `subjectId`/`topicId` became genuinely optional (first content type with no Learn hierarchy home at all) while `subtopicId` carries the article's own id, matching the frontend's pre-existing "subtopicId holds the article id directly" convention for non-Learn bookmarks — zero `bookmarks-list.tsx` changes needed.
  - **Leaderboard is computed live, no `Leaderboard` model writes** (same "live aggregation stands in for a materialized view, no background-job scheduler exists yet" precedent Analytics/Step 47 already set) — new `leaderboard` module (`repositories/leaderboard.repository.ts`, `services/leaderboard.service.ts`, `controllers/`, `validators/`, `routes/leaderboard.routes.ts`) — `GET /leaderboard?period=weekly|monthly|overall`, `GET /leaderboard/me?period=`. **Documented ranking formula** (transparent, never invented): a student needs ≥5 answered questions (`MIN_ATTEMPTS_FOR_LEADERBOARD`) within the period window before ranking at all; `score = correctCount × 10 + round(accuracyPercent)` — real practice volume that was actually correct is the dominant term (can't be gamed by one lucky question), accuracy is a smaller tiebreak-scale term. `weekly` = last 7 rolling days, `monthly` = current calendar month, `overall` = all-time. Display names are privacy-conscious ("First L.", derived from real `Profile.name`, never shown in full) — applies everywhere a ranking is shown, including publicly.
  - **Landing Page Top Rankers now reads the same real source** — new minimal `public` module (`controllers/public.controller.ts`, `routes/public.routes.ts`, unauthenticated) exposing `GET /public/top-rankers`, backed by `leaderboard.service.ts`'s new `getPublicTopRankers()` (overall period, filtered to students with an actual primary exam goal set — never a fabricated fallback category, returns fewer than `limit` rather than padding with anything invented). `PlatformStats`/`Testimonials`/`SuccessStories` stay mocked, out of this step's explicit scope.
  - **Notifications is real end to end** — `NOTIFICATION_CATEGORIES` extended from 4 to 6 (additive: `achievement`, `system-notice` — covering earned-badge notices and both system announcements/global broadcasts; the original 4 unchanged), `Notification.model.ts` gained `scope: 'user'|'global'` (provenance only — a broadcast is still one real document per recipient, per `docs/Database.md` §4.7's fan-out design, so every existing per-user read/unread/delete query needed zero changes) and an optional `actionLabel` (bilingual CTA text, matching what the frontend's `NotificationCard` already needed). New `notifications` module (routes unchanged from the pre-existing illustrative shape in `docs/API.md` §15 — `GET /notifications` [paginated, `isRead`/`category` filters], `PATCH /:id/read`, `POST /read-all`, `GET /unread-count`, plus a new `DELETE /:id` needed to keep the already-working frontend delete button functional). Real, callable trigger functions: `notifyUser` (generic), `notifyAchievementEarned` (genuinely unwired to an automatic trigger — no gamification/badge-awarding backend exists yet, Practice's XP/Coins/Achievements are still honestly 0 per Step 46 — but the category and code path are real, not just seed content), `broadcastSystemNotice` (real fan-out, one document per active `User`).
  - **Frontend**: `services/currentAffairsService.ts`, `services/leaderboardService.ts`, and `services/notificationsService.ts` are all real now — `currentAffairsMockService.ts`/`notificationsMockService.ts` deleted once orphaned; `leaderboardMockService.ts` survives only as a dead import inside `dashboardMockService.ts`'s already-unused `getRankSummary` (not deleted, would break that file's build). Every existing component/type kept its shape — `learnProgressService.ts`'s bookmark dispatch gained a `current-affairs` branch (mirroring the existing `question` branch's direct-id pattern) rather than any UI change; `notificationsService.ts` fetches one generously-sized page and returns a flat array, preserving the exact `Promise<NotificationItem[]>` contract `NotificationList` already expects (no pager UI added — a deliberate "connect to the real API, don't redesign" choice); `leaderboardService.ts`'s two functions gained an additive `period` parameter (zero existing callers before this step, since no Leaderboard UI exists yet, so a safe signature change). `readProgressPercent` stays a purely local `currentAffairsProgressService.ts` concept, unchanged — no backend field for it, an explicitly out-of-scope field this step.
  - **Dev seed**: 13 Current Affairs articles (`backend/src/seed/data/currentAffairs.seed.ts`, adapted from the retired frontend mock's real TNPSC-realistic content — no scraping, per this step's explicit instruction) with dates relative to seed-run time (same "Today/This Week always true" pattern `liveExams.seed.ts` set), spanning daily/weekly/monthly and all 8 categories, several with embedded `quizQuestions`, two linked to real seeded Indian Polity `Question`s via tag lookup for "related questions." No Notification seed data — notifications are inherently per-user and no dev `User` exists at `npm run seed` time; proven instead via the verification script below. Seed confirmed idempotent (identical count on a second run).
  - **Verified against the real Atlas cluster, end to end**: both apps' `typecheck`/`lint`/`build` clean. A temporary, throwaway `tsx` script (written, run, then deleted) bypassed Firebase the same way every prior real-backend step did and drove the real service-layer functions directly against live MongoDB — 38/38 assertions passed: Current Affairs preview/list (daily/weekly/monthly)/detail/quiz/search/archive, a full bookmark on/off round trip with a raw-Mongo cross-check; Leaderboard eligibility (a 3-attempt user correctly excluded, `hasEnoughData: false`), correct descending-score ordering, a high-volume user correctly outranking a higher-accuracy-but-lower-volume one, and public Top Rankers correctly filtering to users with a real exam goal; Notifications category coverage (including the new `achievement`/`system-notice`), a real broadcast reaching all 5 test users, unread-count/mark-one/mark-all/delete all correctly decrementing, and pagination (`limit=2` → 2 items, `totalPages: 2`) — cross-checked against a raw Mongo count. Test data (5 users, profiles, sessions, attempts, bookmarks, notifications) fully deleted after the run; shared seed content (Current Affairs articles, syllabus Questions) untouched.
  - **Not implemented, as instructed**: no external scraping (all Current Affairs content is hand-authored dev seed); no invented leaderboard rankers (empty/small-cohort states are honest, never padded); Community/cross-module Bookmarks/Settings' Help & Support tab remain out of scope.
- [x] **2026-08-03 Real Cloudinary file/image storage** (Sprint 3 Step 50, immediately following the Current Affairs/Leaderboard/Notifications step) — infrastructure, not a new student-facing module. Full detail in `docs/PROJECT_CONTEXT.md` §13/§14; summary:
  - **Credentials verified live before any code was written**: `backend/.env`'s Cloudinary vars had real-looking values already present with a stale "placeholder only" comment — confirmed genuinely live via `cloudinary.api.ping()`, then re-confirmed end-to-end via a real upload+delete round trip through the actual new service (`npm run verify:cloudinary`, new permanent script, `seed/verify.ts`'s "manual verification aid" precedent).
  - **`services/media/cloudinaryUpload.service.ts`** — the one file (besides `config/cloudinary.ts`) touching the `cloudinary` SDK, per `docs/FolderStructure.md`'s one-adapter-per-SDK rule. `uploadBuffer()`/`deleteAsset()`, used by every upload path below. `middleware/upload.middleware.ts` gained three purpose-specific `multer` memory-storage instances (avatar: image/2MB; content image: image/5MB; study material file: image+PDF/20MB), each checking MIME type *and* file extension. `middleware/errorHandler.middleware.ts` gained a `MulterError` branch (previously an opaque 500 on an oversized upload).
  - **Four real upload surfaces**, storing only `{url, publicId, resourceType, format, bytes, width, height}` in MongoDB (new additive fields on `Profile`/`Question`/`CurrentAffair`/`StudyMaterial` — never binary content, per `docs/Database.md`): self-service `POST`/`DELETE /users/me/avatar`, and `content_editor`/`admin`-gated `POST`/`DELETE /questions/:id/image`, `/current-affairs/:id/image`, `/study-materials/:studyMaterialId/file` — the last three are media-attach endpoints on existing read-only content, not a Phase 10 admin-CRUD build-out (`routes/questions.routes.ts` is new and currently holds only this one endpoint pair for that reason).
  - **A real pre-existing security gap closed**: `PATCH /users/me`'s `avatarUrl` previously accepted any client-supplied URL string with zero validation — removed from `validators/user.validator.ts`, replaced by the real upload endpoint's server-side MIME/extension/size checks.
  - **Deliberately reverses `docs/Architecture.md`/`docs/API.md` §11's documented client-direct signed-upload-widget design** in favor of a backend-proxied one — a signed widget can't enforce server-side MIME/extension/size validation before Cloudinary accepts the file, which this step explicitly required; `CLOUDINARY_API_SECRET` still never reaches the frontend either way. See `docs/PROJECT_CONTEXT.md` §13 for the full reasoning; `docs/API.md` §11's `photo-upload-signature` entry was replaced with the two real endpoints.
  - **Frontend deliberately untouched**: the entire Settings/Profile module (`features/settings/`) — not just avatar, name/email too — remains `localStorage`-only; wiring just avatar to the new real endpoint while leaving the rest mocked would have left the page in a confusing half-real state. Flagged as follow-up work, not done partially.
  - **Verified**: backend `typecheck`/`lint`/`format:check`/`build` all clean.
- [ ] Analytics' remaining 14-view drill-down (Subject → Topic → Subtopic, Heatmaps, Consistency calendar, AI Recommendation synthesis card — `Analytics.md` §1-§14) — not started. Accuracy/Speed/Weak/Strong now have real dedicated data (Step 47) but still live inside the single-page dashboard, not their own drill-down views.
- [ ] `layouts/dashboard-layout.tsx` (sidebar + top bar, `InformationArchitecture.md` §4) — not started; the Dashboard page above renders its own minimal chrome-free header directly in the meantime (same simplification `OnboardingPage` already uses).

## Phase 8 — Commerce ⬜

Razorpay integration, webhook-gated subscription activation (`Architecture.md` §6), Payments UI, invoices.

## Phase 9 — Engagement 🟡

- [x] **2026-07-31 Notification Center** (`pages/notifications/notifications-page.tsx`, `features/notifications/`) — a single page at `/app/notifications` (`ROUTES.notifications`), per `docs/InformationArchitecture.md` §7.7 ("a flat list module, no deep hierarchy"), scoped to the 4 categories this session's build instruction named (Exam Reminders, Study Reminders, Premium Updates, Current Affairs Alerts) rather than §7.7's 5 — see `docs/PROJECT_CONTEXT.md` §13 for the exact mapping/deviation, and confirm with the user before extending the category set. Notification center, category filter chips, mark-as-read (single + bulk "Mark all as read"), and delete are all real. One mock service, `services/notificationsService.ts` (12 hand-authored notifications, `localStorage['nalanda-notifications-state']` for read/deleted state — no content-vs-progress file split this time, since that state isn't reused elsewhere). Fixed a real pre-existing dead link while building this: Dashboard's `NotificationsCard` "View All" pointed at `ROUTES.settings('notifications')` (Settings' still-unbuilt preferences tab) instead of this new, correct `ROUTES.notifications`. Browser-verified end-to-end via a temporary Playwright script (installed and removed per-session) — zero console errors.
  - **2026-08-03 update — real backend.** `services/notificationsService.ts` now calls the live `backend/src/routes/notifications.routes.ts` module end to end; `services/mock/notificationsMockService.ts` deleted. Category set extended 4→6 (additive: `achievement`, `system-notice` — the latter also covers global/broadcast announcements). Full detail in the combined Current Affairs + Leaderboard + Notifications entry under Phase 7 above (landed together in one step) and `docs/PROJECT_CONTEXT.md` §14.
- [x] **2026-07-31 Profile/Settings module** (`pages/settings/settings-page.tsx`, `features/settings/`) — a single tabbed page at `/app/settings/:section` (`ROUTES.settings`, same single-plain-route/own-chrome-free-header pattern as Analytics/Notifications), per `docs/InformationArchitecture.md` §7.5. 5 tabs: Profile (default — avatar upload [local-only preview; **backend now real as of Step 50 below, `POST`/`DELETE /users/me/avatar`, frontend deliberately not yet wired — see `docs/PROJECT_CONTEXT.md` §14**], editable name via RHF+Zod, read-only email with a disclosed "Change Email requires OTP re-verification" action, Study Statistics and Subscription Status both reusing Dashboard/Analytics' existing mock numbers), Preferences (the real Light/Dark/System theme control `components/theme-toggle.tsx` had earmarked for this page, plus Language — both wired to the same `useTheme`/`useLanguage` context the Navbar's toggles already use), Notifications (a Switch per the same 4 categories the Notification Center uses — opt-in/opt-out, distinct from that list), Privacy (Export Data, Delete Account behind a confirm `Dialog` — the app's first destructive-confirm dialog), and Security. **Security deliberately has no "change password" control** — this session's build instruction asked for one, but the app is confirmed-passwordless (`docs/Authentication.md`, already confirmed once for the Auth module — see Phase 4 above); asked again, the user confirmed substituting `docs/Authentication.md` §10's actual replacement instead: an Active Sessions list with per-session revoke plus "Log out of all other devices." One mock service, `services/profileService.ts`, deliberately **not** synced with `providers/auth-provider.tsx`'s `AuthUser` (same disconnected-mock-worlds boundary Practice's XP/Coins ledger has with Dashboard). Fixed two pre-existing dead links (Dashboard's `AchievementsCard`/`DailyStudyGoalCard` `ROUTES.settings('profile')` links) and added a Settings gear to `DashboardPage`'s header. `ROUTES.settings`'s param type was loosened from a strict union to a plain `string` to support the route table's `:section` placeholder pattern. Browser-verified end-to-end via a temporary Playwright script (installed and removed per-session) — zero console errors, no app bugs found.
- [ ] Notification-preference dispatch resolver (a real backend checking these prefs before sending), Community/doubt forum, cross-module Bookmarks index, Settings' Help & Support tab — not started.

## Phase 10 — Admin Panel 🟡

`admin/` app scaffold, CMS (content/questions/mock tests/live exams/current affairs), User Management, Institutional/B2B Management, Subscriptions dashboard, Moderation, Audit Logs. Depends on Phase 5 (backend) and benefits from `shared/` existing first (see Open Decisions).

- [x] **2026-08-04 Sprint 4 Step 52 — Admin Portal Foundation, PASS.** The first Sprint 4 step; full report at `docs/SPRINT_4_STEP_52_COMPLETION_REPORT.md`, summary here (also `docs/PROJECT_CONTEXT.md` §14).
  - **Role taxonomy conflict resolved with the user first, not silently.** The build instruction's roles (`student/premium_student/content_editor/admin/super_admin`) matched neither `docs/Authentication.md` §6 nor the real `backend/src/constants/roles.ts` (`user/moderator/content_editor/admin/support`). Chose the user's own preferred **minimal extension**: kept all 5 existing roles unchanged, added one new role (`super_admin`); "premium student" stays `subscriptionTier`-based, never a literal role, preserving `docs/Authentication.md`'s explicit role/tier independence rule.
  - **Backend**: `constants/roles.ts` gained `super_admin` + a new `ADMIN_ACCESS_ROLES` export. New `models/AuditLog.model.ts` (actor id/email/role, action, entityType/entityId, metadata — no secrets, ever) + `repositories/auditLog.repository.ts` + `services/auditLog.service.ts`. New `repositories/adminStats.repository.ts` (parallel real `countDocuments()` across `User`/`Question`/`Subject`/`Topic`/`Lesson`/`PracticeSession`/`LiveExam`/`CurrentAffair`). `user.repository.ts` gained `listForAdmin`/`countByFilter`/`findRecentStudentRegistrations`/`updateStatus`/`updateRole`. New `services/admin/{adminDashboard,adminUsers}.service.ts`, `controllers/admin/*`, `validators/admin.validator.ts`, and `routes/admin/{index,dashboard,users,auditLogs}.routes.ts` mounted at `/api/v1/admin` — `routes/admin/index.ts` applies `authenticate` + `authorizeRoles(...ADMIN_ACCESS_ROLES)` centrally so every current *and future* admin route is unreachable by a plain `user` by construction, with narrower per-route role checks layered on top (role-change is `super_admin`-only with a self-demotion guard, matching this step's explicit instruction).
  - **`admin/` app**: a real, separate Vite/React/TypeScript app (`docs/FolderStructure.md` §3's "separate app, shared UI kit" design — the UI kit itself is hand-rolled and dependency-light for this foundation pass, not a full shadcn install), same design tokens as `frontend/` trimmed to what it needs, own dev port (5180) so both apps can run against the same backend (`backend/.env`'s `CORS_ORIGIN` is now comma-separated). Auth is **the same Firebase project + `POST /auth/google` endpoint + JWT session pattern** `frontend/` already uses, including the same silent-refresh-on-mount `AuthProvider` — nothing about identity verification was reimplemented, only the authorization outcome (`role`) differs. `ProtectedRoute`/`PublicOnlyRoute` guard every `/admin/*` page; a signed-in student sees an honest Access Denied page. Full layout (`Sidebar`/`Topbar`/`Breadcrumbs`, global-search + notifications as disclosed disabled placeholders, `ProfileMenu`) and all 11 requested routes — Dashboard and Users (+ user detail) are real; Content/Questions/Exams/Current Affairs/Live Exams/Subscriptions/Analytics/Settings are honest "coming soon" placeholders (bulk question upload explicitly excluded from this step, per instruction).
  - **Follow-up, same day — "register account for new admins."** Flagged the security question first (a public "create an admin" form is a real privilege-escalation risk) — user chose both safe options: Email/Password sign-in/registration on `admin/`'s login page (still lands as plain `role: 'user'` by default, same as Google), plus a `super_admin`-only **Invite Admin** feature (new `AdminInvite` model/repository/service/routes — `POST/GET /admin/invites`, `DELETE /admin/invites/:id`). Inviting an existing account's email promotes it immediately; inviting a new email creates a pending invite that's **consumed atomically the instant that email first signs in** (a small, careful addition to the one shared `findOrCreateUserFromFirebase` function both apps' login already uses). Every action audit-logged. Verified via a second throwaway script, 12/12 passed.
  - **Verified**: `backend`/`admin`/`frontend` all `typecheck`/`lint`/`build` clean (`frontend/` re-verified to confirm zero impact, not just untouched by inspection). A throwaway `tsx` script (written, run against the live dev server with real signed JWTs for temporary student/admin/super_admin accounts, then deleted — same pattern as every Sprint 3 real-backend step) drove 13 real HTTP assertions covering deny/allow for student vs. admin-staff, the `super_admin`-only role-change boundary (incl. the self-demotion guard), and the audit log correctly recording a role change with no sensitive fields — **13/13 passed**. No browser-automation tool was available in this environment, so `admin/`'s login/guard flow was **not** click-through-verified by the agent — a running `npm run dev` instance was handed to the user for that instead.

- [x] **2026-08-04 Sprint 4 Step 53 — Admin Question Management + Bulk Import, PASS.** Full report at `docs/SPRINT_4_STEP_53_COMPLETION_REPORT.md`, summary here (also `docs/PROJECT_CONTEXT.md` §14). The bulk-question-upload gap Step 52 explicitly left open.
  - **Question CRUD**: `repositories/question.repository.ts` gained `create`/`updateById`/`updateActiveStatus`/`archive`/`restore`/`listForAdmin`/`findByIdForAdmin`/`findByIdIncludingArchived`/`findQuestionTextsBySubtopic`/`bulkInsert` (only 5 read-only functions existed before, for Practice). New `services/admin/adminQuestions.service.ts` + `controllers/admin/adminQuestions.controller.ts` + `routes/admin/questions.routes.ts` (mounted at `/api/v1/admin/questions`), extending `validators/question.validator.ts` with Zod schemas that mirror `Question.model.ts`'s own Mongoose invariants (2-6 options, exactly-one-correct for `mcq_single`, `pyqYear` required when `isPreviousYear`) so bad input gets a specific 400 before ever reaching Mongoose. **Status model reuses existing fields, no new ones**: `isActive` toggle = activate/deactivate; the existing `softDeletePlugin`'s `deletedAt` = archive/restore. Reference validation (exam/subject/topic/subtopic must exist, be active, and be hierarchically consistent) is one shared function reused by both direct CRUD and the Bulk Import parser. List supports search + exam/subject/topic/subtopic/difficulty/language/previousYear/status filters + pagination, plus 4 cascading `meta/*` lookup endpoints for the admin UI's dropdowns.
  - **Bulk Import (CSV + XLSX)**: new `exceljs`/`csv-parse`/`csv-stringify` deps. Deliberately **stateless** (this project's established "no background-job scheduler" precedent) — `POST /import/preview` parses+validates and returns a preview *without writing anything*; `POST /import/confirm` **re-uploads and re-parses the same file** rather than trusting a client-held "resolved" JSON blob back, so a tampered client payload can never inject unvalidated content — only a plain array of approved row numbers round-trips. Validation covers file/column/row/reference/duplicate (both in-file and against MongoDB, via a per-subtopic-cached text comparison) — invalid or duplicate rows are always excluded, confirmed matching Step 53's explicit "never silently import invalid questions" instruction. `insertMany({ordered:false})` gives genuine partial-import semantics. Security: MIME+extension allowlist (`.csv`/`.xlsx` only) + 8MB/2000-row caps on the upload side; on the read side, XLSX cells are read via their computed `.text`/formula-`.result` only, never a raw formula string (formula-injection defense on ingest); the admin frontend's client-generated error-report CSV sanitizes any leading `=+-@` cell before download (formula-injection defense on the export side). A downloadable CSV/XLSX template (with an XLSX "Instructions" sheet) is generated on the fly from one shared column-definition constant (`constants/questionImport.ts`), so the parser and the template can never drift apart on a column name.
  - **Admin frontend**: `pages/questions/{questions-list-page,question-editor-page,question-import-page}.tsx` replace the old placeholder, following `users-list-page.tsx`'s exact table/filter/pagination conventions (no new UI library — only 2 new hand-rolled primitives added, `Textarea`/`Checkbox`, matching `select.tsx`'s existing "no Radix needed" precedent). One create/edit page toggles between an editable form and a live client-side Preview render. The import page is a 3-step wizard (upload → preview with valid/invalid/duplicate tabs and per-row checkboxes → confirm) with an inline error-report CSV download. **A real pre-existing gap fixed in passing**: `admin/` had no `.prettierrc.json` at all (unlike `backend/`), so `npm run format` had been silently defaulting to Prettier's double-quote style since Step 52 and had never actually been run — added the identical config `backend/`'s already uses, then reformatted the whole app back to the single-quote/no-semicolon style every file was actually hand-written in.
  - **Verified**: `backend`/`admin` `typecheck`/`lint`/`format`/`build` all clean. A throwaway `tsx` script (`backend/src/seed/`, run then deleted, same Firebase-bypass pattern proven 8+ times prior) drove the full CRUD lifecycle, a CSV import exercising all three row outcomes (valid / in-file duplicate / DB duplicate / invalid, plus a case-insensitive enum auto-correction), an XLSX import, and confirmed with a real `practice.service.createSession()` call that a newly bulk-imported question is immediately selectable by Smart Practice — and that deactivating it removes it from the eligible pool again. **27/27 assertions passed** against live MongoDB Atlas; all test data (questions, audit logs, the temporary test user, and its practice session) deleted afterward.

- [x] **2026-08-04 Sprint 4 Step 54 — Admin Content Management System, PASS.** Full report at `docs/SPRINT_4_STEP_54_COMPLETION_REPORT.md`, summary here (also `docs/PROJECT_CONTEXT.md` §14). Real admin CRUD for Exams/Subjects/Topics/Subtopics/Lessons/Study Materials/Current Affairs/Weekly Live Exams — the rest of the "coming soon" placeholders Step 52 left behind, besides Questions (Step 53) and Subscriptions/Analytics/Settings (still not started).
  - **Hierarchy CRUD (Exam → Subject → Topic → Subtopic → Lesson)**: all four/five models went from zero mutation functions (seed-only) to full admin `create`/`updateById`/`updateActiveStatus`/`listForAdmin` (+ `archive`/`restore` for every soft-deletable one — Exam has no soft-delete, matching its existing model design). `Topic.examIds` and `Subtopic.subjectId`/`examIds` are **always server-derived from the parent**, never client-supplied, so the denormalized fields these models already had can never drift out of sync. `StudyMaterial` gained its first-ever plain-metadata `create`/`update` (only a Cloudinary file-attach/detach pair existed before, from Step 50) — that pair is reused unchanged, not duplicated, for upload/replace/preview/remove.
  - **Orphan prevention, bottom-up, no cascading writes**: deactivating or archiving a Subject/Topic/Subtopic is blocked with a specific count-bearing error whenever active, non-archived children still exist underneath it (`topicRepository.countBySubject`/`subtopicRepository.countByTopic`, both already existed; `lessonRepository`/`studyMaterialRepository` each gained a new `countBySubtopic`). Deliberately scoped to the strict 1:1 hierarchy only — `Exam↔Subject`'s many-to-many `examIds` relationship was left unguarded (a Subject can belong to several exams, so deactivating one is often genuinely safe), a disclosed scope boundary, not an oversight.
  - **Current Affairs**: gained `create`/`update`/`listForAdmin` and one new field, `publishAt?: Date`, reusing the existing `isActive` on/off switch as the master publish flag (never a parallel draft/published enum) — draft = `isActive:false`; scheduled = `isActive:true` + future `publishAt`; published = `isActive:true` + past/absent `publishAt`; unpublish = `isActive:false`. Every existing student-facing read (`findList`/`findRecentDaily`/`findByMonth`/`search`/`findById`) now additionally gates on `publishAt`, verified live to have zero effect on pre-existing seeded articles (all have `publishAt` genuinely absent). "Tag" and "link questions" needed no new endpoints — `tags`/`examRelevanceTags`/`quizQuestionIds` were already real array fields, now exposed through the update endpoint; linking reuses Step 53's `GET /admin/questions?search=` for the picker.
  - **Weekly Live Exams**: first-ever admin creation path (`repositories/liveExam.repository.ts` had exactly 2 read-only, non-draft-excluding functions before this step). `totalQuestions`/`totalMarks` are **always server-computed** from `questionIds.length`/`marksPerQuestion`, never accepted from the client, so they can never be entered inconsistently. New model field `resultPublication.publishedAt?: Date` — an admin manual-publish-results override, checked first in `liveExam.service.ts`'s `isResultPublished()` (exported and reused, not re-derived) ahead of the existing `immediate`/`scheduled` timing rules; the admin service only allows triggering it once the exam's real, time-derived `effectiveStatus` is `completed` or `cancelled`, so results can never leak to a still-live exam. `create`/`publish`/`cancel`/`publish-results` all write to `AuditLog`.
  - **RBAC reconciled**: the pre-existing Step 50 media routes (`/questions/:id/image`, `/current-affairs/:id/image`, `/study-materials/:id/file`) were missing `super_admin` from their `authorizeRoles(...)` list (only `content_editor`/`admin`) — added, matching every Step 53/54 admin route's three-role baseline.
  - **Admin frontend**: one tabbed `/admin/content` hub page (Exams/Subjects/Topics/Subtopics/Lessons/Study Materials tabs, each a self-contained list+inline-form) replaces the old standalone `/admin/exams` placeholder and folds it in — the sidebar's separate "Exams" entry was removed as redundant. `/admin/current-affairs` and `/admin/live-exams` each got a real list + full editor page (Current Affairs' editor has an Edit/Preview toggle like the Question editor; both editors' "link/select questions" reuse Step 53's admin question search). Study Material's editor includes a real upload/replace/preview/remove widget against the reused Step 50 endpoint.
  - **Verified**: `backend`/`admin` `typecheck`/`lint`/`format`/`build` all clean. A throwaway `tsx` script (Firebase-bypass pattern, proven 9+ times) drove the full bottom-up hierarchy lifecycle (create → blocked archive at each level while children are active → archive children first → archive succeeds → restore), Current Affairs publish/schedule/unpublish/archive with a real read against the live student-facing feed confirming a scheduled article is genuinely invisible and a published one isn't, and the full Live Exam lifecycle (draft invisible to students → publish → visible → cancel → publish-results blocked while upcoming, succeeds once ended) plus the audit log correctly recording the run's key mutations. **37/37 assertions passed** against live MongoDB Atlas; all test data deleted after.

## Phase 11 — Mobile App ⬜

`mobile/` app scaffold (React Native, per `Architecture.md`), consuming the same REST API — no parallel backend.

## Phase 12 — AI Orchestration Layer ⬜

`backend/src/services/ai/` (orchestrator, prompt loader, per-feature handlers, response validator, cache), `prompts/` versioned templates, `backend/src/integrations/aiProvider/`. **Blocked on a real decision: which AI provider/model.** Nothing in the docs commits to one — needed before this phase can start for real.

## Phase 13 — Infra / DevOps / Testing ⬜

- [ ] Install a test runner (Vitest + React Testing Library is the natural fit for this stack) — currently zero automated tests exist anywhere in the repo. **Underlined by Step 51's audit below**: every "automated" verification this project has done so far (Steps 44-51) is a hand-written, throwaway `tsx` script run once and deleted — real and rigorous, but not a repeatable CI-integrated suite. This remains the single highest-leverage unstarted infra item.
- [x] **2026-08-03 Sprint 3 Step 51 — complete end-to-end audit, PASS.** Not a build step — full report at `docs/SPRINT_3_COMPLETION_REPORT.md`, summary here. Read `CLAUDE.md`/`docs/PROJECT_CONTEXT.md`/`docs/MASTER_ROADMAP.md`/architecture docs first, then: (1) a real throwaway E2E script drove every real service-layer function (same functions the HTTP routes call) against live MongoDB Atlas through the complete named user journey — Register→Firebase-Auth(bypassed at token-verification only, same proven pattern as Steps 44-48)→Onboarding→Exam-Selection→Dashboard→Learn→Practice(incl. AI-Explanation-entitlement free-vs-plus check)→Analytics→Live-Exam(a real temporary exam, confirming result-publication gating works correctly)→Current-Affairs→Leaderboard(confirmed the exact `MIN_ATTEMPTS_FOR_LEADERBOARD=5` boundary)→Notifications→Profile(real Cloudinary avatar round-trip)→Logout(confirmed a revoked refresh token can't rotate)→Login-again(confirmed same user resolved, not a duplicate)→**persistence check** (onboarding/progress/bookmarks/history/profile all re-read fresh from Mongo after the simulated re-login) — 57/57 assertions passed; a follow-up script covered the 3 content-management Cloudinary upload paths the first script didn't (question/current-affairs/study-material images) — 6/6 passed. (2) Three parallel research agents audited the database (live Atlas introspection, not just schema reading), mock/dummy/placeholder data (full-repo grep + 4-way classification), and security (13 areas, line-by-line against `docs/Architecture.md`/`docs/Authentication.md`) — see `docs/SPRINT_3_COMPLETION_REPORT.md` §4-§6 for full findings. Zero functional defects found anywhere in Sprint 3's real code. **Two real findings deliberately left unfixed**, per the step's own "do not delete real data automatically" / "do not implement new features" instructions: 2 orphaned `Profile`/`LearningProgress`/`Session` document groups in live Atlas (leftover from a **prior** session's incomplete test cleanup, not this one) awaiting the user's go-ahead to delete; and a handful of Medium/Low-severity recommendations (no upload-route rate limiter, one missing bookmark-delete params validator, several N+1 query patterns, 2 missing-pagination endpoints) logged for a future session, not applied. (3) Both apps' lint/typecheck/format/build re-verified clean — 18 backend + 5 frontend pre-existing (unrelated to Step 50) formatting-drift files fixed mechanically via `prettier --write`, zero logic touched. No automated test suite exists to run (see the unchecked item above). (4) `docs/PROJECT_CONTEXT.md` §13/§14 and this file updated; `docs/SPRINT_3_COMPLETION_REPORT.md` created with a full per-module CODE COMPLETE/AUTOMATED TESTED/MANUALLY VERIFIED/PARTIAL/FAIL classification table. Sprint 4 was **not** started, per instruction.
- [ ] `.env.example` for `frontend/` (the app already hard-requires `VITE_FIREBASE_*`/`VITE_RAZORPAY_KEY_ID`/`VITE_CLOUDINARY_CLOUD_NAME`/`VITE_API_BASE_URL` via `lib/env.ts`'s Zod schema — there's currently no documented way to actually populate them).
- [ ] CI pipeline (`deployment/ci/` per `FolderStructure.md` §13 — lint → typecheck → test → build).
- [x] MongoDB Atlas cluster — real, provisioned, live-verified (Phase 5, 2026-08-01 entry above).
- [x] **Firebase project — now real, provisioned by the user, and fully verified.** `backend/.env`'s `FIREBASE_PROJECT_ID`/`FIREBASE_CLIENT_EMAIL`/`FIREBASE_PRIVATE_KEY_BASE64` and `frontend/.env.local`'s `VITE_FIREBASE_*` were placeholders as recently as the first half of 2026-08-02 (confirmed live during Step 44's own manual verification that same day), then filled in with real values by the user later the same day. Confirmed working two ways: this session decoded the new `FIREBASE_PRIVATE_KEY_BASE64` and got a well-formed PEM, and a live `POST /auth/google` call with a bogus token logged `"Firebase Admin SDK initialized"` then a genuine `auth/argument-error` from Firebase itself (not the old `"malformed key"` failure). The user then independently browser-tested the full stack and confirmed real Email/Password registration/login, Google login, MongoDB user sync, session issuance, Onboarding persistence, and refresh/logout/login persistence all work end to end. **Auth is no longer just "code complete" — it is real, live, and user-verified.**
- [ ] Razorpay test keys — none provisioned yet.
- [x] **Cloudinary account — real, provisioned, live-verified (Sprint 3 Step 50, 2026-08-03).** `backend/.env`'s `CLOUDINARY_CLOUD_NAME`/`CLOUDINARY_API_KEY`/`CLOUDINARY_API_SECRET` (previously commented "placeholder only," which was stale) confirmed live via `cloudinary.api.ping()` and a full real upload+delete round trip (`npm run verify:cloudinary`). Full detail in the Phase 5 entry below and `docs/PROJECT_CONTEXT.md` §13/§14. `frontend/.env.local`'s `VITE_CLOUDINARY_CLOUD_NAME` remains irrelevant/unused — the implementation is backend-proxied, not the client-direct signed-widget pattern that variable was originally intended for (see `docs/PROJECT_CONTEXT.md` §13).
- [ ] Bundle-size follow-up: `App.tsx`'s build currently emits a >1MB main chunk (flagged, not urgent) — a natural side effect of one file importing the entire component library for preview purposes; resolves itself once real routes/pages exist and code-splitting becomes meaningful.

## Phase 14 — i18n ⬜

`i18n/` is still an empty scaffold. `LanguageProvider` (Phase 3) persists a real `en`/`ta` preference, but no translation library (e.g. `react-i18next`) or translated string tables exist yet — every screen built so far is English-only.

---

## Open Decisions Carried Forward

1. **AI provider/model** — not committed anywhere in the docs; blocks Phase 12, worth deciding earlier if AI-adjacent UI copy needs to reference specifics.
2. **`shared/` package timing** — `FolderStructure.md` §4 defines a cross-app `shared/` package for anything `frontend/`/`admin/`/`mobile/` all need. Doesn't exist yet (only `frontend/` exists so far). Stand it up *before* Phase 10 (Admin) or Phase 11 (Mobile) scaffolding begins, not after — retrofitting shared extraction across already-diverged apps is real rework.
3. **i18n library choice** — `react-i18next` is the natural fit but isn't installed; Phase 14 should confirm before wiring the first translated string.
4. **Test runner choice** — Vitest is the natural fit given the Vite toolchain; not yet installed or discussed with the user.

---

*End of Document.*
