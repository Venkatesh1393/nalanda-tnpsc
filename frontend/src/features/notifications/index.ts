/**
 * The Notifications feature's public surface — pages import from here,
 * never by reaching into `features/notifications/components/*` directly
 * (per `features/README.md`, the same convention `features/current-affairs/
 * index.ts` already follows).
 */
export { CategoryFilter } from '@/features/notifications/components/category-filter'
export { NotificationCard } from '@/features/notifications/components/notification-card'
export { NotificationList } from '@/features/notifications/components/notification-list'
